# PikaDesk 智能体接手指南

## 项目使命

PikaDesk 是独立维护、完全免费、本地优先的中国象棋桌面工作台。目标是用公开协议、独立实现和可复验证据提供专业分析、棋谱研究、开局库与授权棋盘实验能力。

## 先读什么

1. `README.md`：产品定位、能力、验证状态与许可证。
2. `docs/SPEC.md`：范围和验收标准。
3. `docs/threat-model.md`：自动化授权与公平使用边界。
4. `docs/third-party.md`：第三方来源和发布阻断项。
5. `task_plan.md`、`progress.md`、`findings.md`：当前工作、证据和未完成事项。

## 不可破坏的工程契约

- 新安装默认本地，核心分析不得依赖云服务或账号。
- 自动输入只允许本人拥有、离线或明确授权的棋盘；公共平台排位目标必须在校准前拒绝。
- 自动化必须维持“截图识别 → 合法性检查 → 引擎决策 → 单步输入 → 视觉确认”闭环。
- 目标身份、DPI、边界、方向、焦点、用户活动或确认结果发生不确定性时必须停止。
- 文件解析有大小、深度、节点数和编码边界；不使用不受限 Java 对象反序列化。
- 外部格式证据不足时拒绝或产生明确警告，不猜测兼容。
- 不提交鲨鱼程序、私有素材、授权数据；不提供会员绕过、公共排位代走或反检测实现。
- 未解决 `yolov11.onnx` 来源和 `chessman.ttf` 许可前，不创建公开二进制 Release。

## 构建与验证

项目使用 JDK 21、Maven Wrapper 和 JavaFX。Windows PowerShell 基准命令：

```powershell
$env:JAVA_HOME = 'C:\path\to\jdk-21'
.\mvnw.cmd --batch-mode verify
.\mvnw.cmd --batch-mode javafx:run
```

当前已知完整基线为 409 个测试、零失败；若干外部资产与本机探针按条件跳过。修改行为前先写失败测试；完成后运行相关定向测试和一次完整 `verify`。不要把旧测试数硬编码为未来成功条件，应同时核对零失败、错误和跳过。

## 代码与文档规则

- 行为变更采用小切片提交，保持每个提交可构建、可回退。
- 重大边界或昂贵决策写入 `docs/decisions/`；不要删除旧 ADR，改变决定时新增 superseding ADR。
- 新增第三方二进制、模型、字体、棋谱样例或代码前，记录来源、固定版本、许可证和 SHA-256。
- 产品能力用测试与 E2E 证据表达；宣传可以坚定，但不得把计划、推断或局部测试写成已经完成。
- 保留 GPL v3 上游版权与许可证，不因独立仓库或独立产品身份淡化衍生关系。

## 远端公开操作

- 默认只允许本地编辑、测试、打包和本地 Git 提交；不得自动执行 `git push`、GitHub Release、Pull Request、Issue、标签或仓库 About/设置变更。
- 只有项目所有者在当前对话中明确说明“可以上传/推送”并指出范围后，才能进行对应的远端写入；一次授权不自动覆盖后续改动。
- 本地已完成但未上传的工作必须报告提交、验证结果和待推送差异，等待新的授权。完整理由见 `docs/decisions/0002-owner-controlled-remote-publication.md`。

## 当前优先级

1. 解决 YOLO 模型和象棋字体的来源/替代方案，关闭公开安装包许可门。
2. 完成 125%/200% DPI 与无人干预 1,000 半回合稳定性矩阵。
3. 补齐 PF/BH 开局库和完整引擎联赛。
4. 公共平台仅提供赛后棋谱导入与分析，不增加实时平台适配。
