# PikaDesk 研究发现

## 已核实的主线基座

### TCHESS / public-XiangQi

- 仓库：<https://github.com/sojourners/public-Xiangqi>
- 本地基线提交：`2d41525095639548059ebd930b0af4d29efc1364`
- 许可证：GPL-3.0
- 技术栈：Java 21、JavaFX 23.0.1、ONNX Runtime 1.19.2、JNA 5.15.0、SQLite JDBC。
- 已存在：UCI/UCCI、引擎分析和对弈、时间/深度/节点限制、PGN/XQF/CBR/TXQ、XQB/PF/BH 开局库、ChessDB、YOLO ONNX 识别、窗口选择、前后台截图与鼠标、动画确认、自动走棋、观战、图片导入识别、跨平台连接器。
- 主要缺口：没有发现自动化测试；多引擎工作台、配置向导、失败恢复、可解释日志、专业分支树和发布体验仍需补强。
- 关键风险文件：`src/main/java/com/sojourners/chess/linker/AbstractGraphLinker.java`，它承载外部棋盘连接状态机，应先用特征测试固定现有行为，再重构。

## 可参考项目

| 项目 | 可借鉴内容 | 使用方式 | 许可证/风险 |
|---|---|---|---|
| VinXiangQi | 窗口句柄、模板配置、YOLO 确认、自动点击配置体验 | 只借鉴交互与状态机思想，重新实现 | GPL-3.0，旧 .NET/ONNX 依赖，不作主线 |
| cn-croissant | 现代分析 UI、MultiPV、分支树、棋谱数据库、引擎对局 | 借鉴产品结构和验收方式 | GPL-3.0 |
| En Croissant | 成熟的多引擎与数据库桌面工作台 | 借鉴引擎管理和数据库 UX | GPL-3.0 |
| atopx/chessboard | Rust/Tauri 截屏 + ONNX 状态机 | 借鉴状态转换；不依赖其缺失模型 | Apache-2.0；模型未随仓库提供 |
| xiangqi-pwa-offline | 已提交的棋盘识别模型、离线 WASM 皮卡鱼、残局 | 评估模型许可与精度后可选用 | MIT；模型仍需逐文件核验来源 |
| Feijiang | UCI/UCCI 进程生命周期、超时与测试思路 | 只借鉴测试场景 | 仓库许可标注不一致，不能复制代码 |
| xqbook | XQB 开局库格式 | 作为格式兼容依据 | 开放实现，使用前再核实具体许可 |

## 鲨鱼公开功能基线

根据鲨鱼官方公开介绍，VIP 对等目标至少包含：广泛平台/通用识别、最多五引擎、智能脚本、合并连接方案、按比分和棋局阶段调节时间、分支棋谱、语音、批量制作开局库、SVG/PNG 棋子、引擎比赛。对等含义是“用户可完成相同任务且有可复现验收”，不是复制私有代码、素材或逐像素界面。

## 许可证与使用边界

- 皮卡鱼源代码是 GPL-3.0；应用分发时必须保留相应义务。
- 皮卡鱼 NNUE 权重另有使用说明：仅限合法用途，明确举例在线作弊的后果由使用者承担；未经许可不得商用。个人非商用并不自动等于可任意再分发，因此安装器设计为用户从官方来源获取或自行指定，并显示来源与哈希。
- 本项目不提供公共平台专用的隐蔽插件、反检测、后台托管排位或规避封禁功能。
- 对本地测试棋盘、自己开发的棋盘、离线棋局和获得明确授权的第三方窗口，可以提供自动走棋。

## 2026-08-26 官方在线核实

- 鲨鱼官方下载页显示最新版本为免费版 1.8.1 / VIP 2.5.2，更新日期 2026-05-17；页面明确 1.X 与 2.X 分开维护，2.X 仅 VIP 用户使用。2.5.2 更新包括识别权重、更多平台兼容、OBK 转 XQB 和自带皮卡鱼更新。
- 鲨鱼官方购买页逐项列出 13 个 VIP 优势：广泛平台支持、天天象棋优化、最多五引擎、智能脚本、连接方案合并、分数步时、前后期时间、时间方案、分支打谱、语音、批量做库、SVG/PNG 棋子、引擎联赛。功能矩阵已经逐项覆盖；对涉及公共真人平台的专用优化，本项目只实现本地/授权环境中的通用技术能力。
- 皮卡鱼官方仓库说明源码采用 GPLv3；分发二进制时需同时提供许可证和生成该二进制的完整源码或其链接。
- 皮卡鱼官方 Networks 仓库单独列出 NNUE 权重许可：合法使用、未经许可不商用。
- Apache Maven 官方文档确认 Windows 使用 `mvnw.cmd`，并支持在 `maven-wrapper.properties` 固定版本和 `distributionSha256Sum`。因此任务 1 将使用官方 Wrapper 的 only-script 方案并固定 Maven 分发包 SHA-256，避免把不必要的 Wrapper JAR 提交进仓库。
- Apache Maven 官方下载页当前推荐稳定版为 3.9.16；3.10.0 与 4.0.0 仍为预览版，因此构建基线固定 3.9.16，不采用预览版。
- JUnit 官方指南建议用 BOM 对齐 JUnit Platform/Jupiter 组件，并至少加入一个 TestEngine；Maven Surefire 3.6.0 起统一通过 JUnit Platform 执行测试。项目仍按已批准规格采用 JUnit 5 稳定线，不在本切片跨到 JUnit 6。
- Maven Central 元数据核实 JUnit 5 最新稳定线为 5.14.4。Surefire 官网已经生成 3.6.0 文档，但 3.6.0 正式构件尚未发布，因此实际固定 3.5.4。
- Adoptium 官方 API 返回 Windows x64 Temurin JDK `21.0.12.1+1`，ZIP 大小 205,073,461 字节；下载后的 SHA-256 与 API 值完全一致。
- Maven 3.9.16 ZIP 的 SHA-512 与 Apache 官方旁文件一致；Wrapper 固定的是在此已验证文件上计算出的 SHA-256。
- 原始 TCHESS 68 个 Java 源文件在 Temurin 21.0.12.1 上编译成功。当前构建有 `jnativehook` 自动模块、过时 API 和未检查操作警告，但没有编译错误。
- 测试框架采用真实 Red/Green：首次运行因缺少 `org.junit.jupiter.api` 产生 5 个预期编译错误；加入 JUnit BOM/Jupiter 后 1 个测试通过，随后 `mvnw.cmd --batch-mode verify` 成功生成 JAR。

## 架构结论

采用模块化单体，保留 JavaFX：

1. `engine`：UCI/UCCI 进程、最多五引擎、MultiPV、超时与熔断。
2. `game`：中国象棋规则、FEN、走法、分支棋谱、时钟与对局状态。
3. `recognition`：截图、棋盘定位、棋子识别、置信度、局面合法性。
4. `automation`：授权目标、坐标映射、落子、视觉确认、紧急停止。
5. `profile`：平台无关的连接配置、棋盘区域、缩放、主题模板、时间策略。
6. `book`：XQB/PF/BH/OBK 查询、批处理制作和质量报告。
7. `desktop`：JavaFX 工作台、日志、配置向导、语音、主题。

## 尚待实测

- TCHESS 当前主分支在 Windows 11、Java 21 上能否无修改编译和启动。
- 已提交 YOLO 模型对不同 DPI、主题、缩放和棋子素材的精度。
- 后台截图/点击在不同渲染技术窗口上的成功率。
- 皮卡鱼官方 Windows 二进制和 NNUE 下载、分发许可的最新细节。
- 鲨鱼免费版的交互细节是否值得在隔离环境中做黑盒体验对照。

## 2026-08-26 随包资源审计

- 仓库中唯一的模型/可执行类二进制是 `src/main/resources/model/yolov11.onnx`，大小 10,575,683 字节，SHA-256 为 `099c4ef0cbfbd07f680037bb1aabf59024f5c0243964b36aeec7c7a57f7213e1`。
- ONNX Runtime 读取到的模型元数据：Ultralytics YOLO11n、导出版本 8.3.17、日期 2024-10-19、输入 `1x3x640x640`、输出 `1x19x8400`、自述许可证为 `AGPL-3.0`。类别为 14 种中国象棋棋子加一个棋盘类别。
- 模型和整个 TCHESS 源码在同一个初始提交 `2d415250...` 一次性导入，没有单独的训练集、训练脚本、模型来源链接或作者说明。AGPL 元数据能说明导出工具/模型文件标注，但不能证明自定义训练数据的来源，因此发布安装包前仍需关闭这一来源缺口。
- `chessman.ttf` 的 SHA-256 为 `016d2e0923bad6c81bd82a32b475cb630551d980c46151f37f85cc77e445089d`；仓库未给出字体单独来源，暂按上游 GPLv3 仓库资源记录，同时列为待上游确认项。
- 当前“关于”对话框只显示 TCHESS、作者 T、版本 1.9；主窗口标题也是 TCHESS。任务 2 将改为 PikaDesk，并明确“基于 TCHESS”而不是抹去上游。
- 上游 V1.6 发布说明只写明“升级使用 yolov11 模型”，没有提供模型下载来源、训练脚本或数据集许可；对模型完整哈希和模型描述的公开检索也没有找到独立来源。
- 本地参考仓库 VinXiangQi 没有提交可替代的 ONNX 权重，只包含早期 YOLO 调用代码和依赖，因此不能填补该模型的来源缺口。
- 实施上把任务 2 分成“2A 产品身份”和“2B 第三方来源/发布门”：2A 可通过代码与测试完成；2B 在模型/字体来源得到确认或替换前保持未通过，避免把开发期可运行误写成可发布。
- `mvn dependency:tree -Dscope=runtime` 实测得到 12 组运行时构件：ONNX Runtime、JNA/JNA Platform、JNativeHook、JavaFX 的 Base/Graphics/Controls/FXML/Media/Swing、SQLite JDBC 和传递依赖 SLF4J；具体版本、许可证和本机 Maven Central JAR 哈希已写入 `docs/third-party.md`。
- 非文本随包资源共 53 个：44 PNG、1 JPG、1 ICO、5 WAV、1 ONNX、1 TTF。`ThirdPartyNoticeTest` 会枚举这些扩展名，要求 `docs/bundled-resources.sha256` 路径集合完全一致并复算每个 SHA-256。
- 上游只写“GPLv3”，没有明确“或任何更高版本”的授权语句，因此 PikaDesk 元数据采用更保守的 `GPL-3.0-only`；README 原有“禁止未经授权用于商业用途”不是 GPLv3 本身的限制，已从 PikaDesk README 移除，模型/权重的单独限制仍单独记录。

## 2026-08-26 桌面启动实测

- `mvnw.cmd --batch-mode javafx:run` 在 Temurin 21.0.12.1+1 上成功打开主窗口；Windows UI Automation 实际读取到标题 `PikaDesk V0.1.0-dev`。
- 帮助菜单已经显示三个明确的 TCHESS 上游入口。关于对话框实际显示 PikaDesk、0.1.0-dev、构建日期、基于 TCHESS 1.9、固定上游 URL 和 `GPL-3.0-only`，控件均可见。
- 启动日志立即打印 ChessDB 返回的着法数据。源码确认新配置默认 `useCloudBook=true` 且 `bookSwitch=true`，初始化棋局会调用 `CloudOpenBook`，通过 `http://www.chessdb.cn/chessdb.php` 发送 FEN。该行为与本地优先原则冲突，必须改为新安装默认关闭并由用户显式开启。
- 关闭测试时 Maven 父会话和 JavaFX 子进程生命周期没有绑定；本次已按已验证精确 PID 正常关闭。后续需要把“关闭父进程时子进程退出”加入启动器/进程生命周期验收。
- 修复后删除 `target/classes/properties` 模拟全新安装，重新运行 JavaFX 并额外观察 15 秒：没有 ChessDB 内容或其他云库结果输出，窗口标题仍正确，正常关闭后 Maven 返回 BUILD SUCCESS。序列化往返测试证明显式保存为 `true` 的旧/用户配置不会被新默认值覆盖。

## 2026-08-26 规则与 FEN 特征测试

- 标准初始局面可以在 FEN 与内部 `char[10][9]` 之间往返，红/黑行棋方字段按 `w`/`b` 输出；无行棋方时只输出棋盘段。
- 当 FEN 中红帅出现在黑将之前时，现有解析器把它视为反向棋盘，并同时反转行序和列序，统一到内部红方在下的方向。
- `validateChessBoard` 会拒绝缺少任一将帅、将帅在九宫外和同色车/马/炮等超过两个等结构错误。
- 已确认的现有限制：`validateChessBoard` 不检查将帅照面，也不拒绝未知棋子字符。这两项暂以特征测试记录，任务 6 的识别边界必须增加更严格验证后再有意更新该测试。
- 马腿、炮架和兵过河横走规则均按现有实现通过；`canGo` 的前两个坐标参数实际表示数组行/列，而非 UI x/y，后续接口重构需避免坐标语义混淆。

## 2026-08-26 引擎协议特征测试

- Windows 临时假引擎实测：`Engine.test` 能在收到 `uciok` 后识别 UCI，并从 `option name Skill Level type spin default 10 ...` 提取默认值 `10`。
- 无响应假引擎在现有两段各 1 秒的 UCI/UCCI 探测后返回 `null`；异常退出也返回 `null`，但会打印写入和关闭已断开管道的 IOException 堆栈，说明错误分类和安静清理仍需改进。
- 假引擎先输出 250,000 字符单行再输出 `uciok` 时没有管道阻塞或测试超时；测试结束后没有遗留 PowerShell 或 `sort.exe` 子进程。
- 现有解析器能从 `info depth 12 multipv 2 score cp 34 time 55 nps 120000 pv b2e2 b9c7` 得到完整 ThinkData，并从 `bestmove b2e2 ponder b9c7` 产生回调；`z9z8` 等越界着法会被忽略。
- 当前探测总超时、进程销毁和输出限制写死在 `Engine` 内，缺少结构化状态/错误回调；后续五引擎注册表需要提供可配置超时、最大行长度和异常退出原因。

## 2026-08-26 连接器棋盘差异特征测试

- `compareBoard` 的现有结果语义已固定：无差异返回 `null`；对手已走且需同步给引擎为 flag 1；引擎已走且需在授权棋盘执行为 flag 2；三处以上“双方格子都非空但棋子不同”直接判为新局 flag 3；多格差异无法唯一归因时为 flag 4。
- 普通移动和吃子都保留正确的 UI 列/行坐标：测试覆盖红车 `a0-a1` 的双向同步和 `a0-a4` 吃子映射。
- 为避免构造 `Robot` 和 ONNX 会话，纯差异计算及 `Action` 只从私有实例成员调整为包内静态测试缝；运行时调用逻辑和返回算法未变。
- 现有 flag 3 判定只依赖差异计数，并未验证新局合法性；flag 4 在连续十次后会退出内层扫描。安全自动化内核不能直接把这些启发式结果当作点击授权。

## 2026-08-26 自动化安全内核

- 新内核严格实现九态闭环：`DISABLED → ARMED → OBSERVING → RECOGNIZED → THINKING → READY → EXECUTING → CONFIRMING → OBSERVING`；非法越级和任一显式校验失败均闭锁到 `PAUSED`。
- 外部输入不接受裸 `Runnable` 直接执行，而是先创建一次性版本化 `EventPermit`；每个低层输入事件都必须再次核验当前状态和许可版本。
- 序列的等待阶段不持锁，因此紧急停止可以把状态改回 `DISABLED`、清除目标授权并作废在途许可；并发测试证明停止前第一个事件可完成，停止后的第二个事件被拒绝。
- 许可只在单个短时系统事件调用期间持锁。后续适配旧 `Robot` 点击逻辑时，移动、按下、释放和延时必须拆开，延时不得包在许可调用中。
- 当前内核尚未接入旧连接器，也没有实际发送鼠标事件；这一步只建立不可绕过的状态和事件闸门，真实坐标与窗口身份复核属于任务 7。

## 2026-08-26 本地自动化测试棋盘

- 新增独立 JavaFX 启动入口 `javafx:run@local-test-board`，不加载账号、云库、识别模型或引擎；运行中检查其 Java 进程的 TCP/UDP 端点均为 0。
- 测试棋盘状态模型严格解析最多 256 字符、10 行×9 列的简化 FEN，方向只改变显示坐标，不改变 UCCI 规范坐标；红黑方向下同一实际走法都记录为 `a0a1`。
- Windows UI Automation 已实际切换黑方在下、高对比度、125% DPI 测试缩放和 400ms 动画；窗口在动画前后分别报告等待和完成状态。
- 通过只投递给该已确认窗口的测试点击，标准局面真实收到并记录 `1. a0a1`；窗口随后正常关闭，Maven 进程返回 `BUILD SUCCESS`。
- 首轮辅助功能切换暴露 JavaFX ComboBox 会短暂产生 `null` 值；三个监听器增加空值保护后二次执行无异常输出。
- 棋盘 Canvas 已暴露为名为“PikaDesk 本地测试棋盘画布”的辅助功能 Image 元素；UI Automation 能读取其精确屏幕边界，不需要依赖猜测坐标定位整个窗口。

## 2026-08-26 可解释识别安全闸门

- `Yolo5Model.DetectResult` 和 `Yolo11Model.processOutput` 已经保存模型真实 `confidence/score`，但旧 `findChessBoard` 只返回布尔值和字符数组，丢弃了棋盘框、逐棋子置信度及模型身份。
- 新 `RecognitionCandidate` 在边界处深拷贝棋盘与置信度网格，并携带输入尺寸/字节数、棋盘范围、棋盘置信度和模型版本；不保存原始截图。
- 新 `RecognitionGate` 默认要求棋盘置信度 ≥0.90、每个已识别棋子置信度 ≥0.80，并限制单边 8192 像素、3200 万像素和 32MiB 输入。
- 缺将帅、棋子超量、将帅出宫、将帅照面、未知字符、置信度形状错误、棋盘越界和非单步局面跳变都有稳定拒绝码与简短原因。
- 一个稳定走法必须恰好表现为两个格子变化，且来源棋子在目标格出现、不能吃己方棋子；这个数量是代码硬约束，不允许由配置放宽。
- 当前闸门尚未接入 ONNX 推理和旧连接器；下一切片需保留检测框真实分数并构造候选，接入前自动化仍保持禁用。

## 2026-08-26 YOLO 结构化候选适配

- 新纯映射器从 YOLO 中心坐标检测框生成棋盘范围、9×10 棋盘和逐格真实置信度；同一格发生多类别碰撞时只保留最高置信度检测，棋盘外中心点被忽略。
- `OnnxModel` 新增结构化 `recognize` 接口；`Yolo5Model/Yolo11Model` 将既有 `DetectResult.score` 原样交给映射器，Yolo11 候选携带随包模型完整 SHA-256。
- 使用未提交的本地测试棋盘截图做真实 ONNX 探针：输入 1136×879、推理约 115ms，棋盘框 `(192,206,439,499)`、棋盘置信度 `0.9791903495788574`，默认安全闸门接受。
- 临时探针测试与截图均留在被忽略的 `target/` 或已删除，没有进入 Git；模型本身的训练来源/许可发布阻断仍未关闭。
- 旧 `findChessBoard` 和连接器尚未切换到结构化接口，因此此切片不改变运行时点击行为；下一步需要设计稳定帧/新局重置后再接入，避免直接破坏旧的新局检测。

## 2026-08-26 识别闸门运行时接入

- `StabilityTracker` 要求初始局面和每次变化后的局面连续出现两帧才提升为稳定局面；中间任何拒绝都会清空候选帧，非连续相同帧不能通过。
- 稳定基线只接受不变局面或一个规范单步；大范围新局跳变继续返回 `POSITION_JUMP`，不会自动学习成新基线。用户重新启动连线时才显式 `reset()` 并重新确认两帧。
- 截图的 8192 单边像素、3200 万像素和 32MiB 估算输入上限现在会在调用 ONNX `predict` 前检查；结构化结果进入闸门后仍会重复核验，避免仅在昂贵推理之后才拒绝异常输入。
- `AbstractGraphLinker` 的实时识别和单张截图识别已改用 `OnnxModel.recognize → RecognitionGate`；拒绝或未稳定时直接返回失败，不进入差异分类和鼠标动作。
- 最近一次结构化结果可通过 `getLastRecognitionResult()` 读取，后续 UI 可显示拒绝码；当前不打印棋盘或截图，避免诊断泄露。
- `recognition` 目录最终保持 4 个生产文件和 1 个测试文件，符合任务 6 的最多 5 文件约束；YOLO 映射测试已合并到同一测试文件。

## 2026-08-26 授权棋盘坐标模型

- 新坐标模型以规范 UCCI `file/rank` 为唯一输入：红方在下时显示行为 `9-rank`，黑方在下时同时翻转列并使用显示行 `rank`，不会沿用旧连接器含义易混淆的裸 `x/y` 参数。
- 校准保存目标身份/版本、客户区屏幕位置与尺寸、棋盘首末交叉点范围、DPI 和方向；执行前当前快照必须逐项一致，目标失焦或不可见也会拒绝。
- 用户活动采用“指针位置 + 单调输入序号”双重快照；即使鼠标活动后回到原点，输入序号变化仍会返回 `USER_INPUT_DETECTED`。
- 棋盘边界必须严格落在客户区内；红黑两种方向的映射结果在返回前再次同时检查棋盘范围和客户区范围。
- 该切片是纯函数模型，尚未读取 Windows 窗口或发送系统输入；下一切片负责实现只面向已人工授权本地测试棋盘的快照适配与一次性事件序列。

## 2026-08-26 Windows 单步授权执行

- `WindowsAutomationTarget` 只绑定外部人工传入的一个 HWND；目标 ID 组合 HWND 与进程 ID，版本使用窗口线程 ID，可识别句柄复用或目标进程变化。
- 每条 `WM_MOUSEMOVE/WM_LBUTTONDOWN/WM_LBUTTONUP` 都在版本化许可生效后重新读取目标、客户区、DPI、焦点、可见性和 `GetLastInputInfo`；发送使用 `SendMessageTimeout(..., SMTO_ABORTIFHUNG, 100ms)`，不移动物理鼠标。
- 发送共 6 条有界消息，点击/移动等待均在许可锁外；首次真实复测分别因用户输入变化和目标未聚焦而在零消息/零走法时停止，旧许可没有被复用。
- 显式重新建立授权并确认前台窗口后，真实离线测试棋盘在 DPI 96、客户区 1120×840、棋盘交叉点范围 432×486 下只收到 `1. a0a1`，安全内核停在 `CONFIRMING`。
- UI Automation 同时读取到“已显示走法 a0a1”；目标进程 TCP=0、UDP=0，随后通过 `CloseMainWindow` 正常退出，Maven 返回 `BUILD SUCCESS`。
- 命令行 E2E 探针只接受精确标题“PikaDesk 本地自动化测试棋盘”，拒绝其他窗口。连续扫描器的旧点击路径尚未切换到新适配器；任务 8 将在加入视觉确认时完成替换，当前新路径仅用于显式单步验收。

## 2026-08-26 视觉确认判定内核

- `MoveConfirmationTracker` 不暴露任何输入发送接口，只能消费目标快照和已经过识别闸门的结果；因此确认失败本身没有点击重试路径。
- 旧局面继续出现或识别帧暂不稳定时保持 `WAITING`；稳定结果恰好等于应用预期合法走法后的棋盘才进入 `CONFIRMED`，其他稳定合法走法为 `POSITION_MISMATCH`。
- 到达单调时钟期限为 `ANIMATION_TIMEOUT`；目标身份、客户区、DPI、焦点、可见性或安全状态变化为 `TARGET_CHANGED`。四种失败都闭锁，终态随后不可被迟到的正确局面反转。
- 暂停态再次误调用执行接口现在只返回拒绝，不再覆盖最初确认失败原因。
- 诊断事件固定只有状态、短原因、耗时、识别拒绝码和模型版本五个字段，不含棋盘数组、图片或截图。
- 真实离线 E2E 先由 YOLO 建立两帧稳定旧局面，执行一次 `a0a1`，再由同一稳定帧闸门确认新局面；结果 `CONFIRMED`，内核回到 `OBSERVING`。UI 日志独立交叉验证只有 `1. a0a1`，TCP/UDP=0，未产生截图文件。
- 连续扫描器仍需在下一切片切换到这个执行/确认组合；在此之前不宣称主界面的旧连线通道已获得新闭环。

## 2026-08-26 连续扫描器安全闭环迁移

- 新 `WindowsMoveCoordinator` 统一拥有一次走法的全部状态迁移、6 条有界消息和稳定帧确认；成功回到 `OBSERVING`，任一失败停在 `PAUSED`，同一个协调器不自动重新授权。
- `AbstractGraphLinker` 的 flag 2 分支已改为传入规范化棋盘、原始视觉棋盘和方向；失败会暂停连续扫描。控制器 `bestMove` 只更新内部棋盘，由扫描器发现差异后进入协调器，不再直接发送输入。
- 旧 `autoClick`、前台 `Robot` 点击、Windows `PostMessage` 点击以及 `GraphLinker` 的公开点击接口均已删除；当前主程序只有 `WindowsAutomationTarget` 能发送鼠标窗口消息，并且每条消息都受一次性事件许可约束。
- 棋盘检测框先按实际截图宽高映射到物理客户区，再计算首末可点击交叉点，因此截图逻辑尺寸与窗口 DPI 物理尺寸不需要假定相等。
- 代码审查发现识别闸门过去只允许红方在下九宫，发生在连接器翻转之前会错误拒绝黑方在下。现在只接受两种连贯方向：标准方向，或双方同时整体旋转 180°；不允许两位将帅各自任意选择九宫。
- 规则合法性始终在红方在下的规范棋盘验证；视觉确认则在原始屏幕方向移动对应格，避免把黑方在下的视觉坐标误交给规范规则函数。
- 协调器假窗口测试覆盖成功、超时不重试、非法确认超时在零输入时闭锁、DPI 比例映射和黑方在下；真实黑方在下 E2E 也得到 `CONFIRMED → OBSERVING`，界面日志仅 `1. a0a1`，TCP/UDP=0，窗口正常退出。

## 2026-08-26 五引擎注册表

- `EngineRegistry` 明确限制最多五个槽位；注册只验证配置、规范化真实路径并计算 SHA-256，不会自动发现、下载或执行任何引擎。
- 启动前再次计算可执行文件哈希，文件在注册后发生变化就以零进程状态失败；每个引擎分别保存路径、参数、UCI/UCCI、Threads、Hash、MultiPV 和 100ms～30s 启动超时。
- `startAll` 通过虚拟线程并行启动槽位；UCI 和 UCCI 使用各自的握手及 `setoption` 格式，收到协议确认与 `readyok` 后才标记 `RUNNING`。
- 槽位分别拥有进程、输入输出和状态，单个进程退出只把自己标记 `FAILED`；五个真实 PowerShell 假引擎中一个以退出码 17 崩溃时，其余四个继续运行。
- 静默引擎在自己的期限内失败并终止，不拖停健康槽位；命令拒绝 CR/LF 注入，启动输出队列固定 512 行且单行上限 16KiB，关闭与启动并发也不会留下进程。首次失败原因保持粘性，不会被随后 EOF 或退出码覆盖。
- Threads/Hash 全局预算在进程创建前原子核算，保证应用不会配置超过用户预算的协议值；它不是对恶意第三方二进制的 OS 级资源沙箱，引擎若忽略协议选项仍需 Windows Job Object 硬限制。
- 当前交付是可测试的后端注册表，尚未替换主界面的单引擎控件；任务 10 将负责局面代次、结果聚合和最多五栏的分析工作区。

## 2026-08-26 多引擎分析内核

- 每次分析生成单调递增的局面代次；各引擎先进入 `WAITING_READY`，只发送 `stop` 和 `isready`。该引擎返回 `readyok` 后才发送新 `position/go`，屏障前的 `info/bestmove` 全部丢弃。
- 引擎屏障彼此独立，慢引擎不会阻止其他引擎开始新局面；再次切换局面会清空当前 PV 并使先前已排队的启动任务因代次不符而失效。
- 解析结果按引擎和 MultiPV 编号保存，包含代次、深度、cp/mate、时间、NPS 和最多 256 步 PV；畸形数字、非法着法或缺少 PV 的行不会进入快照。
- 共识只比较已启用且可见引擎的 PV1 首着，报告赞同数、参与数、一致性和分歧引擎；显示顺序可独立调整，隐藏不停止计算，暂停只向目标引擎发送 `stop`。
- 所有进程写入由单一虚拟控制线程串行执行，公开方法立即返回 `CompletableFuture`；高频结果通知以原子最新快照合并，慢 UI 监听器不会产生无界事件队列。关闭工作区会立即以取消异常完成仍在等待慢管道的操作，调用者不会永久悬空。
- 当前仍是 UI 无关内核；下一切片需要从现有 `EngineConfig` 建立最多五个注册槽并在 JavaFX 中呈现，不在此阶段宣称主窗口已经具有五栏界面。

## 2026-08-26 JavaFX 五引擎工作区与官方皮卡鱼验收

- 主窗口新增“多引擎”页；从已有 `EngineConfig` 取前五项，用户点击后才在虚拟线程校验文件、SHA-256、资源预算并启动进程。启动多引擎前会停止连线与单引擎，停止或失败清理完成后再恢复原单引擎，避免两套控制器同时写棋盘。
- 横向滚动的五栏分别显示运行状态、参与、显示、上移、下移以及 MultiPV；底部汇总 PV1 首着共识和分歧。高频输出先由工作区合并，再由 JavaFX 层二次合并；局面切换不会直接阻塞 UI 线程。
- 代码审查发现停止按钮曾在异步进程尚未完全退出时提前显示“没有后台进程”并允许立即重启；现改为不可重复点击的“正在停止”状态，待全部进程关闭后才恢复控件。UI 更新和异步错误都携带生命周期代次，旧会话不能覆盖新界面。
- 增加完全离线的 `javafx:run@multi-engine-test` 验收入口，通过环境变量接收用户明确选择的本地引擎，不读取或改写持久配置。
- 官方 GitHub 最新 Release 为 `Pikafish-2026-01-02`；下载归档 SHA-256 与 GitHub API 的 `842570...1a49` 一致。本机使用 `pikafish-avxvnni.exe`，先独立验证 `uciok → readyok → bestmove`，再以 10 总线程、320 MiB 总 Hash 同时启动五个进程。
- Windows UI Automation 实测五栏均返回深度 10 的 PV1/PV2；暂停/恢复一栏、隐藏/显示一栏、移动排序和再次分析均有效。分析按钮调用约 3 ms，五引擎运行时 Java 与引擎 TCP/UDP 端点均为 0；停止及直接关窗后 `pikafish-avxvnni.exe` 残留均为 0。
- 官方归档、可执行文件、NNUE 和许可证只保存在仓库外的本地工具目录；PikaDesk 当前不捆绑皮卡鱼。NNUE 许可要求合法、非商业使用，并明确把在线作弊列为禁止示例。

## 2026-08-26 分支棋谱领域模型

- `GameTree` 是可编辑聚合，但每个 `Node` 都是防御性复制集合的不可变记录；注释、评估、子节点或主线变化会生成新节点值，视图持有的旧节点不会被悄悄修改。
- 每个节点使用 UUID 保持身份，保存/加载不重编号。同一父节点再次插入相同 UCCI 走法会复用原节点；`mergeLine` 先在暂存图中验证整条路径，后续任一步非法时不提交前缀。
- 删除以迭代方式移除整棵变例子树；若当前位置被删除则回到其父节点。提升主线会同步把目标变例移到兄弟列表首位，跳转只改变当前节点，不破坏树结构。
- 新的严格局面边界解析 9×10 FEN 和走棋方，复用现有棋子规则并额外检查错方、送将、吃将、双方同时被将以及“非行棋方仍在被将”的不可能状态。每个子节点都保存走后规范 FEN。
- `GameTreeCodec` 使用自定义 `PDTR` 版本 1 格式，不使用可执行对象反序列化；字符串、节点数、子引用和总读写量（64 MiB）都有上限。加载后重新验证唯一父节点、主线、无环、可达性以及每条父子边的走法/FEN，对截断、超限或结构损坏返回 `IOException`。

## 2026-08-26 分支树与评估界面

- `GameTreeViewModel` 只公开行、导航、曲线和关键失误等有界投影；JavaFX 使用按展开路径加载的 `TreeItem`，不会为整棵大树一次创建全部控件。10,000 手压力用例约 0.4 秒完成领域建立与末节点路径恢复。
- 主线用实心圆和粗体表示，变例用空心菱形和斜体表示，注释另有铅笔标记；区别不依赖单一颜色。注释限制 16,384 字符，曲线最多渲染 2,000 个抽样点。
- 节点点击形成独立树导航上下文，棋盘、单引擎、换招分析和多引擎均读取该节点的最终 FEN；旧 `ChessManualHandle` 不被重置，从树上继续走棋也不污染旧格式棋谱。
- 代码审查否决了“点击树节点时重建旧棋谱表”的早期实现，因为这会让用户保存旧格式时丢失未浏览变例。最终实现保留旧棋谱原样，并在用户主动点击旧棋谱记录或新建局面时才退出树导航上下文。
- 引擎输出先按红方视角归一化，再根据节点走后轮到哪方判断刚才的走子方；只有走子方自身局势恶化且相邻两节点都有评估时才列为关键失误。

## 2026-08-26 连接配置向导

- 旧连线参数窗口只有四个数字字段，Windows 选窗回调随后立即创建并武装 `WindowsMoveCoordinator`；新流程把校准扫描放在武装之前，配置取消或任何验证失败都不会创建可发送输入的观察态。
- `ConnectionWizardState` 是无输入 API 的纯状态机：目标、棋盘、显示、模型/时序和干运行必须按顺序完成。干运行以虚拟活动快照调用 `BoardCoordinateMapper`，只返回两个屏幕点；用户随后修改任一字段都会使 UI 重新锁定。
- 配置文件采用自定义 `PDCP` 行格式，不延用旧 `Properties` 的 Java 对象序列化。共享用脱敏导出清空可执行路径和 `dryRun`；本机完整导出保留路径，但导入状态机仍清除授权并要求绑定当前实时目标。
- Windows 目标描述由当前 HWND 获取 PID、进程路径/文件名、窗口类、标题、客户区和 DPI。向导关闭后再次观察同一目标；身份、版本、客户区或 DPI 变化会显示“重新校准”，而不是静默更新基线。
- 连续执行使用批准时的固定校准，且复核识别方向。停止连接也会结束尚未完成的全局选窗监听并恢复光标；异步停止时执行线程使用局部快照，避免清理字段造成空指针竞态。
- 桌面自动化验证时，本地测试棋盘先后被 Edge/ChatGPT 遮挡；两次向导均展示实际命中的进程并保持未授权。这一现象确认 E2E 脚本必须先用实时 `WindowFromPoint` 校验可见命中点，不能信任 UIA 返回的历史画布坐标。

## 2026-08-26 时间策略内核

- 皮卡鱼官方 UCI 入口同时接受 `wtime/btime`、`winc/binc`、`movestogo` 和 `movetime`；完整局时模式会另行计算最优时间、最大时间和 Move Overhead，搜索中还会根据评估下滑、最佳着稳定性及节点投入提前结束。PikaDesk 的确定性外层预算不复制这些内部启发式；后续接线必须明确选择完整局时参数或固定 `movetime`，不能假定两套策略会同时生效。
- `TimeStrategy` 是无时钟、随机数、全局设置或引擎状态依赖的纯函数。输入只含剩余时间、增益、预计剩余步数、开中残局、当前方视角的 cp 比分和 0～100 复杂度；相同输入返回完全相同的不可变决策。
- 默认先保护 `max(250 ms, 剩余时间的 3%)`，仅把其余时间视为可支配；基础份额为可支配时间除以预计步数再加 80% 增益，单步硬上限至少覆盖基础份额但通常不超过可支配时间的 25%。所有乘法使用整数基点和有界运算，`Long.MAX_VALUE` 输入也不溢出。
- 开局/中局/残局、比分绝对值 75/250/700 cp、复杂度 25/50/75、固定余量与 3% 余量交界均有双边参数化用例；决策同时返回基础份额、三个系数、硬上限、安全余量和五条中文解释，供后续界面预览与审计。

## 2026-08-26 安全智能脚本 DSL

- `PDSCRIPT 1` 是严格版本化的行 DSL，不是通用编程语言。事件、字段、比较符、阶段、走子方、通知和动作均使用固定枚举；唯一数值动作 `SET_TIME_SCALE` 限于 25%～200%，不存在自由命令、路径、类名或 URL 参数槽。
- 解析器在分词前限制 64 KiB 总量和 512 字符行长，并限制规则、每规则条件/动作及总指令数；未知版本、未知或多余字段、重复规则和未闭合规则全部拒绝。规范化保存后可确定性往返。
- 执行器只有应用提供的高层 `ActionSink`，默认最多 2,048 步和 250 ms，支持显式取消；预演不调用动作接收方且不读取墙钟，审计序号和内容对同一输入稳定。
- 授权走棋动作在每次派发前读取真实 `AutomationSafetyKernel`，只有 `READY` 且授权仍存在才产生高层请求；脚本拿不到坐标、窗口句柄或 `EventPermit`，最终低层输入仍由原状态机逐事件复核。
- 固定种子 2,000 组 ASCII 模糊输入只会被安全拒绝或规范化往返；命令、PowerShell、路径穿越、网络 URL 和反射类名注入均有参数化拒绝测试。当前缺口是可视化编辑器和主窗口事件接线，因此对照矩阵保持“部分已有（内核通过）”。

## 2026-08-26 PGN 格式适配器

- 国内历史公开规范规定中国象棋 PGN 的 `Game` 首标签、Chinese/WXF/ICCS 三种记谱、FEN、花括号评注和四种终局结果；鲨鱼官网更新说明明确其支持读取 ICCS 且保存默认 ICCS。新适配器先把 ICCS 做成无歧义基线，Chinese/WXF 返回 `UNSUPPORTED_FORMAT`，不猜测转换。
- `ManualDocument` 统一承载合法 `GameTree`、结果和排序元数据。PGN 解析会逐着调用严格走法验证，因此错方、非法棋子移动、送将以及畸形坐标不能进入文档。
- 读取只消费 4 MiB 加一字节；UTF-8、GB18030 与 BOM UTF-16 都使用严格解码。标签、着法、变例深度、注释、警告和总步数分别有上限，重复变例不会被树复用后静默丢失。
- 导入/导出往返保留主线、括号变例、结果、可表示注释和元数据；UUID 不属于外部 PGN 语义。导出使用迭代栈避免长主线递归溢出，并在每次追加时检查输出大小。
- 数字 NAG 暂不表示，导入时返回有偏移的 `UNSUPPORTED_NAG`；含 `}` 的节点注释无法用规范花括号无损保存，导出返回 `UNREPRESENTABLE_COMMENT`。这些缺口均显式报告。

## 2026-08-26 XQF 1.0 格式适配器

- XQStudio 作者 2001 年公开的 XQF 1.0 说明只保证生成/读取版本 `0x0A`，并明确后续版本更复杂；因此新适配器按版本命名并拒绝高版本，不沿用旧类中缺少可信样例验证的加密分支。
- 公开规格确认标题块是 `0x50-0x8f` 的 64 字节块，而非旧实现读取的 128 字节；比赛、日期、地点、棋手、用时、讲评和作者也按各自固定块解析并检查零填充。
- 第 0 步只有有棋步和无棋步两种固定记录；普通记录的第 4 字节必须为零，评注长度为无符号小端 32 位。新实现对所有偏移先做长度检查，不钳制坐标、不接受非 `00/F0` 标志、不在截断时静默结束。
- XQF 1.0 没有行棋方字段且明确暂不支持变着。读取显式报告按红方先行；写入黑方先行或含变例的 `GameTree` 会失败，避免看似成功却改变棋谱语义。
- GB2312 样例字节可由 GBK 严格解码；新读写统一使用严格 GBK 并拒绝替换解码。输入/输出 16 MiB、50,000 步、单评注 16,384 字节均有独立上限，输出超限前不会写入部分文件。

## 2026-08-26 CBR v2 格式适配器

- 仓库旧 `CbrChessManualImpl` 使用 `Files.readAllBytes`、异常后返回 `null`，且没有保存实现；它对行棋方读取了偏移 2112 的单字节，而独立社区实现与两份真实 v2 样例共同指向偏移 2116 的小端 16 位值。新适配器不复用旧解析路径。
- CCBridge 用户手册只证明产品支持 CBR/CBF/CBL、变例、注释与保存，不是二进制格式规范。头字段和记录布局来自固定提交 `walker8088/cchess@de864834…` 的 GPL-3.0 社区实现与样例交叉核对，因此证据标签必须保持“社区反向格式”。
- 社区 `cut_bytes_to_str` 在原始字节上查找未对齐的 `00 00`，UTF-16LE 的 `Test\0` 会在最后一个字符的高位零与终止符低位零之间提前命中，结果变成 `Tes`。新实现只在偶数字节边界识别终止符，真实样例断言完整标题 `Test`。
- 真实 `test2.cbr`（SHA-256 `6897fe17…ea7c64`）包含根注释 `李1` 和两步注释 `李2/李3`；真实 `test.cbr`（SHA-256 `7c1e4417…a193`）含 102 条记录、11 个变例标志，导入后为 103 节点。测试还核对 `h7h0` 的两个直接子着 `c2d4/e3e4`，不是只统计节点数。
- 第二记录字节并非恒为零：复杂样例中有 10 个值为 1，且与若干分支节点相关，但现有来源不足以给出稳定语义。新读取器接受 0/1、对 1 返回一次 `UNSUPPORTED_STEP_ATTRIBUTE`，其他值拒绝；写出统一使用 0，避免伪造未知属性。
- 读取与写出均限制 16 MiB、50,000 步、256 层嵌套变例和 16,384 字符注释；所有坐标与局面进入 `GameTree` 规则验证。写出已通过内部语义往返和字段级断言，但未在官方 CCBridge 应用中黑盒验证，因此固定返回 `UNVERIFIED_EXTERNAL_WRITER`，不宣称外部写入兼容已经闭环。

## 2026-08-27 TXQ 安全迁移

- 固定提交中的 `TxqChessManualImpl` 没有独立二进制规格：它直接用 `ObjectInputStream/ObjectOutputStream` 序列化 `ChessManual` 与递归 `ManualRecord`。旧格式没有结果字段，且原实现对任意输入调用无限制 `readObject()`。
- 迁移读取器只允许上述两种模型、`ArrayList`、`Integer/Number`、`String/Object` 及必需数组，并同时限制输入 16 MiB、图深 256、引用 100,000、数组 50,000；类白名单违规和对象图资源拒绝返回不同错误。对象形成后仍要经过环/共享引用、主线、节点数、FEN 和合法着法验证。
- 新保存格式为 `PDTX 1`：固定魔数、版本、结果、规范化元数据和有界 `GameTreeCodec` 树，不使用 Java 反序列化。旧界面保存改为完整验证后写同目录临时文件，再原子替换或安全回退，非法模型不会截断已有文件。
- `GameTreeCodec` 原先会把畸形 UTF-8 静默替换为 `U+FFFD`；现改为严格解码并报告错误。TXQ 的元数据与树文本、损坏头、版本、长度、尾随数据和输出上限均有回归。
- 截至 2026-08-27 没有找到独立真实旧 TXQ 文件；测试对象流由精确上游类名与 `serialVersionUID=1` 的模型生成。因此兼容结论限定为当前 TCHESS 类结构，并明确新版 `PDTX 1` 不能由旧 TCHESS 打开。

## 2026-08-27 四格式主界面接线

- 原 `ChessManualHandle` 虽能选择四种扩展名，却仍路由旧服务：PGN 会猜编码并跳过未知内容，CBR 读取整文件且保存为空实现，服务失败返回 `null/void` 后界面仍会继续更新当前文件。格式内核完成并不等于用户真的用上了安全路径。
- 新 `ManualFileGateway` 统一路由四个有界适配器，并在目标文件同目录完成临时写入与原子替换。主界面只有网关成功返回后才更新标题和当前文件；失败以对话框显示，已有目标不会被先截断。
- `ChessManual` 没有结果和任意标签字段，因此控制器另存当前 `ManualDocument.Result` 与隐藏元数据。界面五个字段覆盖相应标签，其余标签跨格式保留；PGN 的 `Event` 与界面标题进行显式映射。
- CBR 候选写出、XQF 先行方假设和旧 TXQ 迁移等通知已到达界面对话框。最多显示前 8 条和余数，避免大量 NAG/兼容提示产生不可用弹窗。
- 真实 JavaFX 启动读取到主标题和棋谱菜单六项；最终关闭使皮卡鱼收到 `quit`，Maven `BUILD SUCCESS` 且无 Java/引擎残留。系统原生保存对话框在多屏 DPI 下未稳定进入 UI Automation 树，故只记录菜单级烟雾，不声称完成格式下拉的人工点击验收。

## 2026-08-27 D 盘迁移、官方复核与目标用途闭锁

- PikaDesk 已从原工作区迁至 `D:\象棋\PikaDesk`。Windows 的首次跨盘移动形成拆分状态；合并前两边无同名文件，剩余 1,231 个文件复制后逐个 SHA-256 一致，最终目标共 2,265 个文件、513,847,984 字节，提交仍为 `988f7f9` 且 `git fsck --full` 通过。旧副本已清理，鲨鱼原目录未被移动或覆盖。
- 鲨鱼官方当前下载页显示 1.8.1/2.5.2，2.x 仅 VIP；2.5.2 更新公开写明模拟器 JJ 连线、识别权重、OBK/XQB 和皮卡鱼更新。官方帮助列出最多五引擎、脚本、分支棋谱、开局库、引擎联赛等功能。这些公开工作流用于任务级对照，不复制二进制、资源或内部实现。
- 本地 `ChessShark.exe` 与备用 EXE 均为 2.5.2.0 且无 Authenticode 签名；主程序 SHA-256 为 `1E75653E…5385C1`，备用为 `788545FE…E560C5`。当前主机找不到 `WindowsSandbox.exe`，查询可选功能又需要提升，所以本轮没有运行这两个未知签名程序，也没有读取或修改授权数据。
- JJ 官方服务协议明确把脚本、机器人软件以及模拟操作等能影响游戏操作的外部程序定义为外挂作弊。因此“公共平台排位自动走棋”不是学习用途的功能对等项；可接受替代为本地人机/引擎联赛、用户自建或明确授权测试棋盘，以及用户主动导出后的 PGN/XQF/CBR/TXQ 赛后分析。
- `ConnectionWizardState` 现在在校准之前调用独立目标策略，对 JJ/天天象棋已知进程、路径或窗口标识失败闭锁；本地测试棋盘的原有授权流程保持不变。该匹配只是额外安全后备，不能把窗口字符串当成授权证明。
- 迁移还暴露出 Surefire 3.5.4 的 forked module-path 在本机 Windows/GBK 中文项目路径下损坏路径。改用 Surefire 官方的 class-path 启动选项后仍保留 forked JVM，目标安全测试先红后绿，随后在 `D:\象棋` 下 336/336 回归通过。
