<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.sojourners.chess</groupId>
    <artifactId>pikadesk</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>PikaDesk</name>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <javafx.version>23.0.1</javafx.version>
        <javafx.maven.plugin.version>0.0.8</javafx.maven.plugin.version>
        <junit.version>5.14.4</junit.version>
        <maven.surefire.version>3.5.4</maven.surefire.version>
    </properties>

    <!-- Keep all JUnit Platform and Jupiter modules on one tested release line.
         Source: https://docs.junit.org/5.14.4/user-guide/#running-tests-build-maven-bom -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>

        <dependency>
            <groupId>com.microsoft.onnxruntime</groupId>
            <artifactId>onnxruntime</artifactId>
            <version>1.19.2</version>
        </dependency>

        <dependency>
            <groupId>net.java.dev.jna</groupId>
            <artifactId>jna-platform</artifactId>
            <version>5.15.0</version>
        </dependency>

        <dependency>
            <groupId>com.1stleg</groupId>
            <artifactId>jnativehook</artifactId>
            <version>2.1.0</version>
        </dependency>

        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-media</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-swing</artifactId>
            <version>${javafx.version}</version>
        </dependency>

        <!-- https://mvnrepository.com/artifact/org.xerial/sqlite-jdbc -->
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>3.45.2.0</version>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>


    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.10.1</version>
                <configuration>
                    <source>${maven.compiler.release}</source>
                    <target>${maven.compiler.release}</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>${javafx.maven.plugin.version}</version>
                <configuration>
                    <jlinkImageName>PikaDesk</jlinkImageName>
                    <launcher>pikadesk</launcher>
                    <mainClass>Xiangqi/com.sojourners.chess.Main</mainClass>
                </configuration>
                <executions>
                    <execution>
                        <id>local-test-board</id>
                        <configuration>
                            <mainClass>Xiangqi/com.sojourners.chess.testboard.LocalTestBoardApp</mainClass>
                        </configuration>
                    </execution>
                    <execution>
                        <id>multi-engine-test</id>
                        <configuration>
                            <mainClass>Xiangqi/com.sojourners.chess.testboard.MultiEngineTestApp</mainClass>
                        </configuration>
                    </execution>
                    <execution>
                        <id>book-batch-test</id>
                        <configuration>
                            <mainClass>Xiangqi/com.sojourners.chess.testboard.BookBatchTestApp</mainClass>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Surefire discovers the Jupiter engine through the JUnit Platform.
                 Source: https://maven.apache.org/surefire/maven-surefire-plugin/examples/junit-platform.html -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven.surefire.version}</version>
                <configuration>
                    <!-- The forked module-path launcher corrupts non-ASCII project
                         paths on this Windows/GBK host. Surefire's documented
                         class-path mode preserves D:\象棋 while retaining a forked JVM. -->
                    <useModulePath>false</useModulePath>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
