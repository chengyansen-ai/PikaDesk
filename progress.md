# PikaDesk 进度日志

## 2026-08-26

- 完成社区与官方项目初筛，确定 TCHESS 为主线基座。
- 将 TCHESS 提交 `2d41525095639548059ebd930b0af4d29efc1364` 克隆到正式输出目录。
- 将官方仓库记录为 `upstream`，创建 `feature/pikadesk-foundation` 工作分支。
- 检测到当前机器 PATH 中没有 Java 与 Maven；尚未安装或更改系统环境。
- 明确不破解鲨鱼，不修改其免费版，不复用私有二进制或素材。
- 开始创建规格、功能对照、威胁模型、实施计划和任务清单。
- 在线核实鲨鱼官方版本页、皮卡鱼源码/权重许可和 Apache Maven Wrapper 官方用法。
- 完成产品规格、VIP 逐项对照矩阵、STRIDE 威胁模型、依赖图与 24 个可验收任务。
- 鲨鱼官方购买页列出的 13 项 VIP 优势已全部映射到功能矩阵。
- 用户明确回复“继续执行”，规格门通过；进入可复现工程基线任务。
- 下载并校验 Temurin JDK 21.0.12.1+1 与 Apache Maven 3.9.16；全部工具仅位于工作区临时目录，没有全局安装。
- 用 Apache Maven Wrapper Plugin 3.3.4 生成 only-script Wrapper，固定 Maven 3.9.16 和分发包 SHA-256。
- 原始 TCHESS 68 个 Java 源文件在 JDK 21 上编译成功。
- 新增 JUnit 5.14.4 测试入口；烟雾测试先因缺少 JUnit 失败，再在配置依赖后通过。
- `.\mvnw.cmd --batch-mode verify` 成功：1 个测试通过，零失败/错误/跳过，生成 `target/Xiangqi-1.0-SNAPSHOT.jar`。
- `target/`、`.tools/`、`.jdks/`、本地日志、密钥和环境文件已加入 `.gitignore`。
- 完成 PikaDesk 产品身份切片并提交 `8533522`：Maven 坐标、JAR 名、主窗口、关于页、导出文件名和棋谱署名已统一品牌化，同时保留 TCHESS 上游入口。
- 产品元数据测试经历 Red/Green：先因 `ProductInfo` 不存在失败；首版实现又准确抓到上游 URL 大小写错误，修复后通过。
- 新增 `NOTICE.md`、第三方依赖表和 53 个非文本资源的 SHA-256 清单；许可证测试先因文件不存在失败，补齐后 2/2 通过。
- 明确两个发布阻断项：`yolov11.onnx` 缺训练来源，`chessman.ttf` 缺独立作者/许可；开发可继续，但不可据此制作公开安装包。
- 真实启动 JavaFX 主窗口并通过 Windows UI Automation 验证主标题、帮助菜单和关于页完整内容；开发会话随后正常关闭。
- 启动时发现上游首次配置默认访问明文 HTTP ChessDB 云库；已定位到 `Properties` 的两个默认开关，列为立即修复的本地优先安全回归。
- 以 Red/Green 测试把新安装的云库/库招默认值改为关闭，同时验证用户显式开启后的序列化值仍保留；删除临时配置后的二次启动 15 秒内没有 ChessDB 输出并正常退出。
- 新增 5 个规则/FEN 特征测试：固定标准局面往返、反向 FEN、非法将帅/超量棋子，以及马腿、炮架、兵过河规则；同时显式记录将帅照面和未知字符仍被旧校验接受的缺口。
- 新增 5 个 Windows 假引擎/解析器特征测试：覆盖 UCI 选项、无响应、异常退出、25 万字符输出、正常 PV/bestmove 与非法着法；测试后无子进程残留。
- 新增 5 个连接器差异特征测试，固定无动作、对手同步、引擎点击、新局和疑似新局五类结果；只开放纯计算测试缝，没有触发 Robot、ONNX 或鼠标事件。
- 以两轮 Red/Green 完成自动化安全内核：先固定九态闭环，再把整段动作锁细化为版本化逐事件许可，避免点击延时阻塞紧急停止。
- 完成本地自动化测试棋盘：支持 FEN、红黑方向、两种主题、75%～200% DPI 测试缩放、0～2000ms 动画和实际 UCCI 走法日志。
- 真实 JavaFX/UI Automation 验收先发现并修复下拉框瞬时空值异常；复测切换全部配置、接收 `a0a1`、动画完成、零网络端点并正常退出。
- 为测试棋盘 Canvas 增加辅助功能名称和 Image 角色；UI Automation 已读取精确边界，供后续安全校准 E2E 使用。
- 新增可解释识别候选、结果与纯安全闸门；确认旧 YOLO 已计算真实分数但被布尔接口丢弃，实际推理适配留在下一切片。
- 完成 YOLO 结构化映射和实际 `OnnxModel.recognize` 接口；真实本地棋盘截图推理得到 0.97919 棋盘置信度并通过安全闸门，临时探针未提交。
- 实时连接器已消费结构化识别闸门：初始/变化局面要求连续两帧，大跳变拒绝，用户重新启动连线才重置基线；拒绝帧不进入动作分类。
- 识别资源上限已前移到 ONNX 推理前，并在结构化闸门中再次核验，超大或无效截图不会触发模型推理。
- 建立授权棋盘纯坐标模型：统一 UCCI 坐标、红黑方向、客户区/棋盘双边界，并在映射前复核目标身份、窗口几何、DPI、焦点、可见性和用户输入活动。
- 完成 Windows 授权单步适配器：每条有界窗口消息都在一次性许可内重新校验；失焦、用户活动、句柄复用和发送失败均经测试闭锁。
- 真实离线 E2E 的两个未满足前置条件分别在零落子时拒绝；重新授权后严格记录一次 `a0a1`、停在 `CONFIRMING`、零网络端点并正常退出。
- 完成视觉确认判定内核：预期稳定局面成功；动画超时、局面不一致、目标变化均闭锁且不重试，终态不可被迟到帧反转。
- 真实 YOLO 闭环 E2E 已从两帧旧局面、授权 `a0a1` 到两帧新局面确认完整跑通；内核回到 `OBSERVING`，UI 日志仅一条且没有截图落盘。
- 新协调器已接管 Windows 主扫描器的 flag 2 路径；引擎回调只更新内部棋盘，旧直接点击 API 及其 `Robot`/`PostMessage` 实现均已删除，停止或失败会清除授权/暂停扫描。
- 修复黑方在下会被九宫规则提前拒绝的问题，并把规范规则坐标与原始视觉坐标分离；真实黑方在下 YOLO 闭环只走一次 `a0a1` 并确认成功。
- 完成最多五槽的本地 UCI/UCCI 引擎注册表：注册不启动进程，路径规范化后记录 SHA-256，启动前复核文件未变化，并按协议独立发送 Threads、Hash、MultiPV。
- 注册阶段原子核算全局 Threads/Hash 配置预算；五个真实 PowerShell 假引擎并行握手，单个退出码 17、静默超时和关闭竞争均未影响健康槽位，测试后残留假引擎进程为 0。
- 完成多引擎分析内核：逐引擎以 `stop/isready/readyok` 建立局面代次边界，旧局面的迟到 `info/bestmove` 不会进入新快照；UCI/UCCI 风格分数、MultiPV、PV、深度、时间和 NPS 均结构化保存。
- 工作区支持独立暂停/恢复、排序、隐藏以及首着共识/分歧；进程管道写入在虚拟控制线程串行执行，UI 通知只保留最新快照，慢引擎或慢监听器不阻塞调用线程也不会形成无界通知积压。
- 主窗口新增可访问的“多引擎”页：用户显式启动后显示最多五栏 MultiPV、参与/显示/排序控件和共识摘要；启动、分析、停止都不会在 JavaFX 线程做文件哈希或进程 I/O。
- 从皮卡鱼官方 Release 下载并校验 `Pikafish-2026-01-02`，归档 SHA-256 与 GitHub API 一致；AVX-VNNI 构建在本机完成 `uciok/readyok/bestmove` 实测，文件与 NNUE 只留在仓库外本地工具目录。
- 新增不污染持久配置的五引擎离线验收入口；五个真实皮卡鱼进程同时返回深度 10 的 PV1/PV2，共识/分歧、暂停恢复、显隐、排序和快速再次分析均通过，运行期间 TCP/UDP 端点为 0。
- 代码审查修复异步停止窗口：清理完成前按钮保持禁用并显示“正在停止”，进程归零后才宣告停止；旧会话 UI 通知和取消异常不能覆盖新会话状态。停止及关窗后子进程均为 0。
- 以 Red/Green 完成分支棋谱领域模型：不可变节点、稳定 UUID、主线/变例、注释、cp/杀棋评估、跳转、提升主线、整棵删除和原子合并均已实现。
- 新增严格 FEN/走法边界；错方、畸形着法、棋子规则错误、送将、吃将和不可能的被将状态不能写入树。
- 新增有界 `PDTR` v1 原生保存格式；往返保留节点身份、当前位置、主线、注释和评估，读写总量限制为 64 MiB，加载时拒绝截断、重复身份、过量引用、环、不可达节点及父子局面不一致。
- 主窗口新增可访问的“分支树”页：懒加载层级以实心主线、斜体变例和注释标记呈现，并提供节点注释、提升主线、确认后删除整棵变例、主线评估曲线和关键失误跳转。
- 节点点击以独立分析上下文恢复精确 FEN；单引擎常规/换招分析与多引擎都读取同一上下文，旧格式棋谱不会因树导航被重置或改写，从树节点继续走棋也只写入新分支树。
- 引擎 PV1 评估写入当前节点；关键失误只在相邻已评估节点间按实际走子方判断，避免把好棋或跨越未评估节点的波动误报为失误。曲线超过 2,000 点时等距抽样显示。
- 单步插入改为常数级树更新；10,000 手循环棋谱的建立、根子节点查询和末节点路径恢复在测试中约 0.4 秒完成。
- 完成五步连接配置向导：真实窗口身份、自动/手填棋盘边界、方向、DPI、主题、模型与时序被合并为一次明确授权；干运行只计算坐标，不调用任何输入 API。
- Windows 连线器不再在选窗后立即武装。只有向导完整通过、返回配置与当前目标再次逐项匹配后才创建点击协调器；停止/取消会注销全局监听并恢复系统光标。
- 运行时固定使用批准时的客户区、DPI、棋盘边界与方向；窗口移动、缩放、句柄/进程变化、失焦、用户输入或识别方向不一致都会闭锁，不用新的现场值覆盖旧校准。
- 新增有界 `PDCP` v2 配置格式和 v1 迁移：64 KiB 上限、严格字段集、重复/未知字段拒绝，不使用 Java 对象反序列化。脱敏导出移除本机程序路径和授权状态；本机导入也必须重新选窗、干运行和授权。
- 真实 JavaFX 向导验收显示五段内容完整；没有目标时干运行/导出/授权均锁定。两次被遮挡的选窗分别准确识别为 Edge 与 ChatGPT，均未误当本地棋盘或武装点击，说明窗口身份来自实际 HWND/进程而非预期标题。
- 完成纯函数时间策略内核：比分、开中残局、剩余时间、增益、预计步数和复杂度共同决定目标预算；决策包含基础份额、硬上限、安全余量、各项整数系数和中文解释。
- 时间预算始终先扣除 `max(250 ms, 3%)` 安全余量，再把目标限制在可支配时间与单步硬上限内；极短时间和 `Long.MAX_VALUE` 输入均经参数化测试验证无越界或溢出。
- 完成 CBR v2 社区格式审计：交叉检查 CCBridge 用户手册、`walker8088/cchess` 固定提交、两份真实 CBR 和仓库旧解析器，明确区分产品能力说明、社区反向布局与已验证样例。
- 新增严格有界 `Cbr2Adapter`：支持 UTF-16LE 元数据/根注释/逐着注释、任意合法初始局面、红黑行棋方、结果、主线和嵌套变例；损坏头、编码、长度、棋盘、坐标、着法、重复分支及尾随数据全部带代码和偏移拒绝。
- 两份 GPL-3.0 真实样例以 Base64 和完整 SHA-256 固定在测试中。复杂样例 102 条记录导入为 103 节点，并核对首个二分支父子关系；真实记录属性值 1 被接受并显式报告未知展示语义。
- CBR 写出使用 16 MiB 原子有界缓冲，内部往返保留变例、行棋方、结果、元数据和注释；由于尚未在官方 CCBridge 应用中黑盒验证，每次导出都会返回 `UNVERIFIED_EXTERNAL_WRITER`，不会过度声称兼容完成。

### 本轮验证

- `git status --short --branch`：干净，分支为 `feature/pikadesk-foundation`。
- `git rev-parse HEAD`：`2d41525095639548059ebd930b0af4d29efc1364`。
- `pom.xml`：Java 21、JavaFX 23.0.1、ONNX Runtime 1.19.2、JNA 5.15.0。
- Maven Wrapper：3.9.16，分发 SHA-256 已固定。
- 测试：JUnit 5.14.4 + Surefire 3.5.4，1/1 通过。
- `verify`：产品身份切片通过，3/3 测试成功并生成 `target/pikadesk-0.1.0-SNAPSHOT.jar`；`target/` 被 Git 忽略。
- 资源许可证测试：2/2 通过，逐文件复算 53 个随包二进制资源的 SHA-256。
- 基线特征测试：15/15 通过（规则/FEN、引擎协议、连接器棋盘差异各 5 项）。
- 完整 `verify`：100/100 通过，生成 `target/pikadesk-0.1.0-SNAPSHOT.jar`；`git diff --check` 无错误。
- 主窗口迁移后启动检查：标题 `PikaDesk V0.1.0-dev`，观察 3 秒 TCP/UDP=0，正常关闭并由 Maven 返回 `BUILD SUCCESS`。
- 自动化安全内核：8/8 通过，覆盖默认禁用、正常闭环、全阶段失败闭锁、越级、发送失败、空序列、停止后拒绝和并发许可失效。
- 本地测试棋盘状态模型：5/5 通过，覆盖 FEN、方向坐标、移动、吃子、走法记录及配置边界。
- 识别与 YOLO 结构化边界：12/12 通过，覆盖元数据、深拷贝、置信度、非法局面、资源上限、稳定帧、显式重置、真实分数、冲突选择和棋盘框。
- 授权棋盘坐标：7/7 通过，覆盖红黑方向、目标/DPI/客户区复核、用户活动和全部 90 个交叉点边界。
- Windows 授权目标：5/5 通过，覆盖 6 消息单步、逐事件失焦/用户活动、句柄复用和有界发送失败。
- 视觉确认判定：6/6 通过，覆盖稳定成功、超时、错步、目标变化、终态不可逆和无截图诊断字段。
- Windows 走法协调器：5/5 通过，覆盖红/黑方向成功、超时零重试、非法确认超时零输入和截图/DPI 到客户区换算。
- 识别闸门：新增整体旋转局面测试，标准与黑方在下两种连贯方向均可进入稳定帧流程。
- 五引擎注册表：6/6 真实子进程测试通过，覆盖 UCI/UCCI、独立资源选项、SHA-256、五槽/预算、启动超时、崩溃隔离、命令换行拒绝、并发关闭和 16KiB 单行输出上限。
- 多引擎分析工作区：6/6 通过，覆盖两代局面隔离、MultiPV 排序、共识/分歧、独立启停/排序/隐藏、慢管道立即返回、100 次高频结果通知合并和关闭取消。
- JavaFX 五引擎验收：15/15 相关测试通过；官方 Pikafish 五进程 E2E 的五栏 PV1/PV2、暂停/恢复、显隐、排序、共识、停止门、零网络和零进程残留全部通过。
- 分支棋谱领域模型：8/8 通过，包含插入/复用、主线/变例、跳转、子树删除、整行合并原子性、注释/评估/UUID 往返、非法输入拒绝和流式超限文件拦截。
- 分支树视图模型：7/7 通过，覆盖主线/变例投影、精确路径与子招上下文、注释/评估/首着及后续关键失误、未评估间隙、导入前完整验证、10,000 手压力和主窗口宿主契约。
- JavaFX 分支树验收：两次真实启动均为 `BUILD SUCCESS`；UI Automation 读到树、图例、注释、曲线与失误列表，点击根节点后棋盘从两步后局面准确恢复初始局面并显示引擎上下文切换状态；TCP/UDP=0，关窗后 PikaDesk Java 进程为 0。
- 时间策略参数化测试：50/50 通过；完整 `verify` 为 156/156，生成 `target/pikadesk-0.1.0-SNAPSHOT.jar`。
- 安全脚本 DSL 定向测试：35/35 通过；覆盖严格语法往返、危险能力注入拒绝、资源上限、固定种子模糊输入、确定性预演、实时派发、步数、取消、超时、异常脱敏和全部非 `READY` 自动化状态；完整 `verify` 为 191/191。
- 新棋谱适配器第一片 PGN：19/19 通过；覆盖 ICCS 主线/变例/结果/元数据/注释往返、黑方先行编号、GB18030、损坏语法、非法着法、结果冲突、超大流、编码损坏、警告上限和不可表示字段；完整 `verify` 为 210/210。
- 新棋谱适配器第二片 XQF 1.0：28/28 通过；覆盖公开样例头与坐标、GBK 元数据/评注、任意合法初始局面、主线往返、固定第 0 步、结果/类型、流式输入上限、原子输出上限、损坏/截断/编码错误，以及高版本、变例、黑方先行和不可表示字段的显式拒绝/报告；完整 `verify` 为 238/238。
- 新棋谱适配器第三片 CBR v2：25/25 定向测试通过；覆盖两份真实 CCBridge 样例、UTF-16LE 对齐解码、102 步多变例树、元数据/结果/行棋方/注释往返、17 类损坏输入、流式输入上限和原子输出上限。
- CBR 切片完成后的完整 `mvn verify` 为 263/263，零失败、错误或跳过，并生成 `target/pikadesk-0.1.0-SNAPSHOT.jar`。
- 新棋谱适配器第四片 TXQ：移除旧界面的无限制对象反序列化；新增只读旧 TCHESS 迁移白名单、对象图资源过滤、迁移后合法性校验，以及不使用 Java 序列化的 `PDTX 1` 安全保存格式。
- TXQ 23/23 定向测试通过，覆盖安全格式完整树往返、精确旧模型迁移、未授权类、深层图、环/共享节点、主线/重复分支、严格 UTF-8、损坏/超大输入、原子输出上限，以及旧界面保存失败不截断已有文件。
- TXQ 切片完成后的完整 `mvn verify` 为 286/286，零失败、错误或跳过，并生成 `target/pikadesk-0.1.0-SNAPSHOT.jar`。
- 新增 `ManualFileGateway` 并将主界面 PGN/XQF/CBR/TXQ 打开、保存、另存为和目录双击统一接到有界适配器；旧文件服务不再承担磁盘棋谱路径。
- 界面会话保留对局结果和隐藏元数据；标题等五个可编辑字段覆盖原值，其他标签跨格式保存。失败不改变当前文件、不截断目标，兼容通知以最多 8 条加余数的警告框呈现。
- `ManualFileGatewayTest` 10/10 通过，覆盖四格式大小写扩展名往返、主线/变例、结果、中文着法、PGN Event 映射、隐藏标签、CBR 警告、未知扩展名、XQF 拒绝时原文件/临时文件安全，以及主界面不回退旧服务。
- 主界面真实启动可访问棋谱菜单并正常关闭，皮卡鱼收到 `quit`，Maven 返回 `BUILD SUCCESS`，无 Java/引擎进程残留；系统原生保存对话框受多屏 DPI 自动化树限制，未作为逐项点击通过证据。
- 界面接线后的完整 `mvn verify` 为 296/296，零失败、错误或跳过，并生成 `target/pikadesk-0.1.0-SNAPSHOT.jar`。

## 2026-08-27

- XQB v1 批量建库、跨进程恢复、本地自动化真实红/黑单步 E2E、406 半回合耐久安全停止，以及 Windows 开发 app-image 已在提交 `988f7f9` 前完成；迁移前完整测试为 335/335。
- 把整个工程迁入 `D:\象棋\PikaDesk`。首次跨盘移动部分完成后，清点源/目标为 0 个同名冲突；剩余 1,231 个文件逐个 SHA-256 复制一致，`git fsck --full`、干净工作树和应用 EXE 均验证后，永久清理旧副本。
- 只读核验本地鲨鱼两个 2.5.2.0 EXE：均无数字签名；当前主机未提供 Windows Sandbox，未直接运行、未改授权文件。
- 在线复核皮卡鱼、TCHESS、鲨鱼官方公开功能和 JJ 官方服务协议；继续以任务级功能对等为目标，但公共平台排位代走、实时辅助、专用连接方案和反检测明确不实现。
- 新增公共排位目标失败闭锁测试：测试先证明 JJ/天天目标可进入向导而失败，再加入目标策略使其在校准前被拒绝；本地与明确授权的测试棋盘流程不受影响。
- 修复迁移后的中文路径测试问题：Surefire forked module-path 会把 `D:\象棋` 损坏为替换字符，切换到官方 class-path 模式后保留 forked JVM并恢复测试加载。
- 中文路径下完整 `mvn verify` 为 336/336，零失败、错误或跳过，并生成 `target/pikadesk-0.1.0-SNAPSHOT.jar`。
- 从 `D:\象棋\PikaDesk` 重新生成 Windows 开发镜像：223 个文件、223,723,149 字节；222 项 SHA-256 清单全部复算一致，隐藏启动后进程响应正常、网络连接为 0，并在冒烟结束时精确退出。
- 以 `main` 为默认分支在 GitHub 公开源代码仓库：`https://github.com/chengyansen-ai/PikaDesk`。README、项目历程、变更记录、贡献指南、安全政策、第三方归属和 GPL-3.0-only 说明均随源码发布，不包含内部对话内容。
- 首次推送暴露本地基线为浅克隆；从官方 TCHESS 上游补全到 98 个提交后，缺失对象检查、`git fsck --full`、历史敏感文件名和常见密钥模式复审均通过，随后用完整对象包成功上传全部历史。
- GitHub 已启用 Issues 和私密漏洞报告，关闭 Wiki，并配置中国象棋、Pikafish、TCHESS、JavaFX、UCI/UCCI、本地优先等主题。未创建二进制 Release：YOLO 模型训练来源和象棋字体许可仍是公开二进制发布阻断项。
- 按独立产品口径完成第二次开源整理：README 改为“问题—契约—能力—实施链—证据—结构—立场—许可证”，新增 `AGENTS.md` 与 ADR-0001，并把原有 99 提交完整备份为仓库外 Git bundle；公开 `main` 将以包含全部当前源码和文档的独立根提交重建，同时继续保留 TCHESS 固定基线与 GPL v3 归属。

### 下一步

1. 在隔离环境可用时，只做鲨鱼 2.5.2 公开菜单/布局/操作路径对照，不登录、不联网、不测试授权绕过或平台连线。
2. 继续审计 PF/BH 的真实样例与许可证；证据充分的格式按单格式切片补齐批量导入、去重和校验，证据不足则明确拒绝而非猜测兼容。
3. 补语音、引擎联赛、125%/200% DPI 与无人干预 1,000 半回合；创建正式二进制 Release 前解决 YOLO 模型来源和字体许可阻断。
4. 公共平台只增加赛后导出棋谱的导入说明与分析报告，不增加实时识别、自动输入或排位适配。

## 2026-08-28

- 定位截图中“引擎未加载”的直接原因：默认配置没有任何引擎，旧打包脚本也明确不携带引擎；新增显式、本机专用的 `local-assets` 首启配置，源码发行仍保持安全离线默认。
- 接入 Pikafish 官方最新稳定 Release `Pikafish-2026-01-02` 的 AVX-VNNI 构建与官方 NNUE；归档、EXE、NNUE 和许可证文件均记录 SHA-256，二进制不提交 Git。
- 修复中文安装路径下绝对 `EvalFile` 被原生引擎错误解码的问题；最终引擎输出明确确认 12 线程、1 GiB Hash、MultiPV 3 和 `NNUE evaluation using pikafish.nnue`。
- 开局知识接入官方 ChessDB。未捆绑没有明确再分发许可的社区“最强棋库”；五次标准开局查询均为 HTTP 200 且包含合法着法，平均 803 ms。
- 修复后台开局库查询直接更新 JavaFX 控件的线程竞争；协议、云库全文和正常网络超时日志改为默认静默、显式诊断开关。
- 本机普通单步默认改为固定时间 1.5 秒；仍支持界面调节和无限分析。
- 最终 app-image 五次启动：窗口平均 1,786 ms、最大 1,841 ms；引擎进程平均 1,227 ms、最大 1,306 ms。
- 最终真实界面 E2E：显示 `Pikafish 2026-01-02 AVX-VNNI` 与 `固定时间1.5秒`；云库出现 18 个可见来源单元；点击分析后出现深度、PV、NPS，窗口保持可交互，退出后应用和引擎残留进程为 0。
- 完整 Maven 回归 338/338 通过，Windows 开发镜像为 229 个文件、278,482,224 字节。
- 已创建并复核桌面快捷方式 `C:\Users\MSI\Desktop\PikaDesk.lnk`，目标和工作目录均指向最终 app-image。
- 性能结论与限制记录于 `docs/performance-2026-08-28.md`；不把单机测试夸大为所有硬件、网络条件下的“全面优化”。
