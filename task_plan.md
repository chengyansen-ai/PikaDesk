# PikaDesk 总任务计划

## 目标

在 TCHESS（`public-Xiangqi`）与皮卡鱼的公开接口之上，独立实现一款完全免费、本地优先的中国象棋桌面工具，逐项达到鲨鱼 VIP 已公开功能的可验证对等效果。自动走棋只服务于本人、本地、离线或明确授权的棋盘环境；不破解鲨鱼、不绕过许可证、不为公共平台排位作弊提供预设或规避能力。

## 当前状态

- 当前阶段：独立根仓库、智能体接手指南和宣传型项目叙事已经完成；XQB v1、授权自动化 E2E 与 Windows 开发镜像已完成，下一阶段为 PF/BH 开局库、语音、引擎比赛、资源溯源和剩余稳定性矩阵
- 代码基线：TCHESS `2d41525095639548059ebd930b0af4d29efc1364`
- 工作分支：`main`，公开仓库为 `https://github.com/chengyansen-ai/PikaDesk`
- 规格门：用户已于 2026-08-26 回复“继续执行”，已通过

## 阶段

| 阶段 | 状态 | 退出条件 |
|---|---|---|
| 0. 基线与研究 | 完成 | 上游、许可证、公开功能和参考项目有可追溯记录 |
| 1. 产品规格与安全边界 | 完成 | 规格、对照矩阵、威胁模型可由用户审阅 |
| 2. 实施计划与任务切片 | 完成 | 每项任务均有依赖、验收和验证命令 |
| 3. 可复现工程基线 | 完成 | 项目级 JDK 与 Maven Wrapper 已完成构建、测试及 JavaFX 启动验证 |
| 4. 核心分析工作台 | 完成 | 多引擎、分支棋谱、局面分析可用 |
| 5. 识别与授权自动走棋 | 已完成 | Windows 主扫描器使用授权单步、稳定视觉确认和失败闭锁；红黑方向真实 E2E 通过 |
| 6. VIP 工具集补齐 | 进行中 | 五引擎与分支棋谱已完成；其后完成时间方案、脚本、开局库、比赛、语音和主题 |
| 7. 稳定性与发布 | 进行中 | Windows 开发镜像已通过；1,000 半回合、模型许可和正式安装包仍待闭环 |
| 8. 鲨鱼黑盒界面对照 | 待环境 | 只在隔离环境记录公开交互；当前主机未启用 Windows Sandbox，且本地 2.5.2 EXE 未签名，故未运行 |

## 核心决策

1. 以 TCHESS 为主线基座，保留 GPLv3 许可证和上游归属，不从鲨鱼二进制复制实现。
2. 皮卡鱼作为独立 UCI/UCCI 引擎进程接入；不修改引擎许可证或神经网络权重许可。
3. Windows 10/11 x64 为首发平台；保留 TCHESS 的跨平台接口，但不让跨平台阻塞首版。
4. 外部窗口控制默认关闭；目标必须人工选择并加入本地允许列表，且始终提供可见的停止开关。
5. 识别与自动落子采用“截图识别 → 合法性检查 → 引擎决策 → 落子 → 截图确认”的闭环，任何不确定状态都停止。
6. 鲨鱼免费版仅可在 Windows Sandbox 中作为黑盒交互参考；禁止修改、反编译、注入、绕过授权或复用其私有素材。
7. PikaDesk 新安装必须默认离线；云库等网络能力只能由用户明确开启，并显示目标服务与发送的数据类型。

## 错误与阻塞记录

| 日期 | 问题 | 尝试 | 处理 |
|---|---|---|---|
| 2026-08-26 | 系统 PATH 中无可用 Java 21 与 Maven | 直接探测 `java`、`mvn` | 计划在任务 1 引入 Maven Wrapper 和项目级 JDK 引导，不修改全局环境 |
| 2026-08-26 | `python` 命令被 Windows Store 别名截获 | 运行 planning-with-files 的 session-catchup | 改为后续使用 Codex 随附 Python；当前新仓库无旧会话需要恢复 |
| 2026-08-26 | 任务拆解技能引用的 `references/definition-of-done.md` 不存在 | 按引用路径读取一次 | 在本项目规格中定义等价 Definition of Done，不重复读取缺失路径 |
| 2026-08-26 | 一次联网搜索脚本因嵌套双引号产生 JavaScript 语法错误 | 1 次 | 改为单引号查询并成功取得鲨鱼官方购买页，不重复原写法 |
| 2026-08-26 | 首次暂存检查发现 Markdown 行尾空格和多余 EOF 空行 | `git diff --cached --check` | 对本轮 Markdown 做一次限定范围的机械清理，重新暂存后复检 |
| 2026-08-26 | 仓库未配置 Git 作者身份，首次提交被拒绝 | 1 次 | 不修改用户全局配置；本提交使用命令级临时身份 `Codex <codex@localhost>` |
| 2026-08-26 | 首次生成 Wrapper 时 PowerShell 将未引用的 `-Dmaven=3.9.16` 拆成错误参数 | 1 次 | 改为逐参数单引号传递；成功生成固定 Maven 3.9.16 的 Wrapper |
| 2026-08-26 | Maven Central 不提供 Maven 发行 ZIP 的 SHA-256 旁文件 | 1 次 | 先用官方 SHA-512 验证 ZIP，再计算并固定 Wrapper 所需 SHA-256 |
| 2026-08-26 | Surefire 官方站已出现 3.6.0 文档，但 Maven Central 尚无 3.6.0 正式构件 | 1 次 | 固定已发布稳定版 3.5.4；不采用 3.6.0-M1 预览构件 |
| 2026-08-26 | 首次并行下载没有保留长运行会话标识，遗留两个 Maven curl 进程写同一临时文件 | 1 次 | 精确停止两个已确认的 curl 进程，从 Maven Central 官方仓库重新下载并通过 SHA-512 验证 |
| 2026-08-26 | 提交前秘密扫描命中官方 Wrapper 中空的 `MVNW_PASSWORD` 变量名 | 1 次 | 人工确认变量没有值；业务文件继续严格扫描，Wrapper 通过官方生成来源和差异审阅验证 |
| 2026-08-26 | PowerShell 资源哈希脚本在 `foreach` 代码块后直接接管道，产生“空管道元素”解析错误 | 2 次 | 固定改为先用 `$rows = foreach (...)` 收集对象，再单独排序输出；后续不再拼接这种管道 |
| 2026-08-26 | 首次补写规划文档时补丁上下文误把 Markdown 列表项写成表格行 | 1 次 | 读取原文后用准确上下文重试成功，未造成文件损坏 |
| 2026-08-26 | JavaFX UI Automation 的 `Expand/InvokePattern` 没有稳定展开“帮助”弹出菜单，且一次打开了相邻菜单 | 2 次 | 先用 UIA 读取精确元素边界，再只对本项目窗口按边界中心点击；成功验证关于页，不使用猜测坐标 |
| 2026-08-26 | 在 Maven 批处理会话发送 Ctrl+C 只结束父会话，JavaFX 子进程仍运行 | 1 次 | 校验 PID 1588 的可执行路径与完整命令行后调用 `CloseMainWindow()`，3 秒内正常退出；未结束其他 Java 进程 |
| 2026-08-26 | 一次多文件补丁虽然目标行存在，但组合上下文校验失败 | 1 次 | 拆成按文件的较小补丁后成功；未产生部分写入 |
| 2026-08-26 | JavaFX ComboBox 经 UI Automation 选择时短暂产生 `null`，三个监听器输出异常堆栈 | 1 次真实界面验收 | 为方向、主题、缩放监听器增加空值保护；完整重复相同 UIA 流程后终端无异常 |
| 2026-08-26 | 一个补丁对 `task_plan.md` 声明了两个更新段，补丁工具拒绝整个请求 | 1 次 | 合并为每个文件单一更新操作后重试；首次失败未产生写入 |
| 2026-08-26 | PowerShell 把未引用且含逗号的 `-Dtest=...` 当作参数列表解析 | 1 次 | 把整项 Maven 属性作为单引号参数传递，针对性测试随后 23/23 通过 |
| 2026-08-26 | CBR 真实分支样例的 Base64 常量在首次转录时缺失尾部有效位 | 1 次 | 从固定提交样例重新生成完整常量并用 SHA-256 断言锁定，未绕过真实性检查 |
| 2026-08-26 | CBR 真实样例否定了“记录第二字节恒为零”和测试中的根注释字面值 | 1 轮证据复核 | 允许真实出现的 0/1 并显式报告未知属性语义；断言改为原文件 UTF-16LE 实值 `李1/李2/李3` |
| 2026-08-27 | 跨盘 `Move-Item` 遇到隐藏 Git 元数据后只完成了部分目录 | 1 次 | 分别清点两边，确认 0 个同名冲突；复制剩余 1,231 个文件并逐文件 SHA-256 校验，`git fsck`/工作树通过后清理旧副本 |
| 2026-08-27 | 普通递归删除和 Windows 回收站接口均拒绝旧副本 | 3 次 | 先清除旧副本属性并复核 ACL/占用；回收站仍不可用，最终只对已验证的精确旧路径永久清理，目标和鲨鱼文件未触碰 |
| 2026-08-27 | 迁移后当前终端未设置 `JAVA_HOME` | 1 次 | 复用既有项目级 Temurin 21，只设置当前测试进程环境，不修改系统配置 |
| 2026-08-27 | Surefire fork 在 `D:\象棋` 下把模块路径转成 `D:\����`，测试类无法加载 | 1 次 | 按 Surefire 官方配置切换到 forked class-path 模式；中文路径下 336/336 全量回归通过 |
| 2026-08-27 | 首次向新 GitHub 仓库推送时，服务端因浅克隆缺少预期上游对象而拒绝对象包 | 1 次 | 从官方 TCHESS 上游补全完整历史，执行 `git fsck --full` 和历史敏感文件名复审后，以非 thin 对象包成功推送 98 个提交的完整历史 |

## 质量门

- 规格已获用户确认；后续范围变更先更新规格再实施。
- 每个行为变更先有失败测试，再写最小实现。
- 每个切片必须测试、构建、手工检查后独立提交。
- 不把模型、引擎、下载器或自动化目标的许可问题隐藏在“开源”二字后面。
- 不把“界面相似”当作“功能对等”；以 `docs/shark-vip-parity-matrix.md` 的证据为准。
- 公共平台排位客户端在目标选择阶段即失败闭锁；平台只支持用户主动导出的棋谱做赛后复盘，不提供实时分析或自动输入。
