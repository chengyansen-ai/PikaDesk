# PikaDesk 任务清单

## 0. 规格批准（XS）

**说明：** 用户审阅 `docs/SPEC.md`、功能矩阵和安全边界。

**验收：**
- [x] 用户确认项目名或给出新名称：暂用 PikaDesk。
- [x] 用户确认自动化范围和优先顺序：按已批准规格执行。

**验证：** 在本任务中记录明确批准及任何修改。
**依赖：** 无。
**文件：** `docs/SPEC.md`、`tasks/plan.md`、`tasks/todo.md`。

## 1. 可复现构建与测试入口（M）

**说明：** 增加 Maven Wrapper、JUnit 5 和固定 JDK 21 的本地引导，先证明原始基线可构建。

**验收：**
- [x] 无全局 Maven 的 Windows 机器可运行 `mvnw.cmd test`。
- [x] 至少一个故意先失败再修复的烟雾测试被记录。
- [x] 构建产物与本地工具目录不进入 Git。

**验证：** 2026-08-26 已运行 `.\mvnw.cmd --batch-mode test` 和 `.\mvnw.cmd --batch-mode verify`；1 个测试通过，零失败/错误/跳过，JAR 生成成功。
**依赖：** 0。
**文件：** `pom.xml`、`mvnw`、`mvnw.cmd`、`.mvn/wrapper/*`、`.gitignore`。

## 2. 身份、许可证与来源（S）

**说明：** 独立品牌化，同时完整保留 TCHESS/GPLv3 归属并增加第三方组件清单。

**验收：**
- [x] 关于页实际显示 PikaDesk、TCHESS 上游和许可证，并已通过 Windows UI Automation 检查控件可见性。
- [ ] 每个随包二进制/模型都有来源、许可证、版本和 SHA-256。
- [x] 不包含鲨鱼私有资源。

**验证：** 许可证清单测试 + 手工查看关于页。
**依赖：** 1。
**文件：** `README.md`、`LICENSE`、`NOTICE.md`、`docs/third-party.md`。

### 2A. 产品身份（XS）

**验收：**
- [x] 可测试的产品元数据显示 PikaDesk、版本、TCHESS 上游和 GPL-3.0。
- [x] 主窗口、关于页、导出文件名和棋谱署名不再把新产品冒充为原版 TCHESS。

**验证：** 产品元数据单元测试 + 完整构建；JavaFX 关于页在启动验证阶段手工检查。

### 2B. 第三方来源与发布门（S）

**验收：**
- [x] `NOTICE.md` 保留 TCHESS 归属并明确与鲨鱼无隶属关系。
- [x] `docs/third-party.md` 逐项列出代码依赖、随包模型/字体的版本、许可、来源和 SHA-256。
- [x] 来源不完整的资源明确阻止发布打包，直至上游确认或替换。

**验证：** 文档审阅 + 资源哈希复算；来源未关闭时任务 2 总验收保持未通过。

### 2C. 首次启动隐私默认值（XS）

**验收：**
- [x] 新创建的配置默认关闭云库和库招，不在启动时发送 FEN。
- [x] 已保存的用户选择仍按原值加载，不用新默认值覆盖。
- [x] JavaFX 启动日志不再出现 ChessDB 返回内容。

**验证：** 配置默认值单元测试 + 删除测试启动产生的临时配置后再次启动观察。

## 3. 基线特征测试（M）

**说明：** 固定现有规则、FEN、UCI/UCCI 和连接器关键状态，防止重构破坏行为。

**验收：**
- [x] 规则和 FEN 有正常、边界、非法输入测试；已记录旧校验仍接受将帅照面和未知字符的限制。
- [x] 假引擎覆盖超时、异常退出、超长输出和正常 PV；已记录异常退出仍打印管道堆栈的限制。
- [x] 连接器现有棋盘差异状态（无动作、同步引擎、授权点击、新局、疑似新局）已由纯计算特征测试固定。

**验证：** `.\mvnw.cmd --batch-mode test -Dtest=*CharacterizationTest`。
**依赖：** 1。
**文件：** 最多 5 个测试/夹具文件；不改行为代码。

## 4. 自动化安全内核（M）

**说明：** 用显式状态机隔离授权、观察、思考、执行、确认和暂停。

**验收：**
- [x] 默认 `DISABLED` 状态不会启动外部事件序列，也不能发送低层事件。
- [x] 任一显式校验失败或非法越级进入 `PAUSED`；禁用状态保持 `DISABLED`。
- [x] 紧急停止清除授权并以版本化许可原子禁止所有后续事件，包括在途序列的下一事件。

**验证：** `AutomationSafetyKernelTest` 8 项状态机、故障闭锁与并发停止测试。
**依赖：** 3。
**文件：** `automation` 下不超过 5 个生产/测试文件。

## 5. 本地测试棋盘（M）

**说明：** 提供不连接公共平台的确定性窗口，用于截图、DPI、动画和鼠标 E2E。

**验收：**
- [x] 可设置 75%～200% DPI 测试缩放、红黑方向、经典/高对比主题和 0～2000ms 动画速度。
- [x] 可应用简化 FEN 目标局面，并记录实际收到的 UCCI 走法、吃子和动画状态。
- [x] 独立启动入口不需要网络和账号；实测 Java 进程 TCP/UDP 端点均为 0。

**验证：** `LocalTestBoardStateTest` 5 项测试 + `javafx:run@local-test-board` + Windows UI Automation 实际执行 `a0a1`。
**依赖：** 1。
**文件：** 独立 test-app 模块，不超过 5 个首批文件。

## 6. 可解释识别结果（M）

**说明：** 识别输出包含局面、置信度、边界、模型版本和拒绝理由。

**验收：**
- [x] 非法棋子数量、缺将帅、将帅照面、非单步局面跳变和低置信度都有结构化拒绝码。
- [x] 棋盘/棋子置信度直接来自 YOLO 检测分数并可测试；真实本地棋盘截图以 0.97919 棋盘置信度通过。
- [x] 输入限制为单边 8192 像素、3200 万像素、32MiB，棋盘框还必须完全位于输入图像内。

**验证：** `RecognitionGateTest` 12 项夹具/资源/稳定帧/YOLO 映射测试 + 未提交真实 ONNX 探针。
**依赖：** 3。
**文件：** `recognition` 下不超过 5 个生产/测试文件。

## 7. 校准与单步落子（M）

**说明：** 人工授权本地测试棋盘，建立坐标映射并只执行一个走法。

**验收：**
- [x] 窗口身份、棋盘边界、方向和 DPI 在执行前复核。
- [x] 坐标永不越过棋盘或目标客户区。
- [x] 用户鼠标活动或目标失焦时停止。

**验证：** 7 项坐标测试 + 5 项 Windows 逐事件测试 + 本地测试棋盘真实 `a0a1` E2E（含零落子拒绝与零网络端点）。
**依赖：** 4、5、6。
**文件：** `automation`/`profile` 下不超过 5 个生产/测试文件。

## 8. 视觉确认闭环（M）

**说明：** 每次落子后等待稳定新局面，并与预期着法比较。

**验收：**
- [x] 成功、动画超时、局面不一致和窗口移动都有确定状态。
- [x] 失败后不自动重试点击。
- [x] 诊断事件说明暂停原因但不泄露隐私截图。

**验证：** 6 项确认判定 + 5 项协调器故障/方向测试 + 红/黑方向真实 YOLO 单步闭环；100 半回合压力验收保留在任务 20。
**依赖：** 7。
**文件：** 不超过 5 个生产/测试文件。

## 9. 五引擎注册表（M）

**说明：** 管理最多五个 UCI/UCCI 进程及全局资源预算。

**验收：**
- [x] 每引擎独立路径、协议、线程、哈希、MultiPV 和启动超时。
- [x] 注册的 Threads/Hash 配置总额不能超过用户预算。
- [x] 单个引擎崩溃不影响其他引擎。

**验证：** 6 项真实子进程集成测试：五个 PowerShell 假引擎并行 UCI/UCCI 握手、独立选项、崩溃隔离、启动超时、容量/预算、二进制篡改、并发关闭与超长输出；测试后残留进程为 0。
**依赖：** 3。
**文件：** `engine` 下不超过 5 个生产/测试文件。

## 10. 多引擎分析工作区（M）

**说明：** 同一局面对齐显示最多五个引擎的 MultiPV、共识和分歧。

**验收：**
- [x] 分析内核与主界面均可独立暂停/恢复、排序和隐藏引擎。
- [x] 切换局面使用逐引擎 `stop → isready → readyok` 屏障，不会混入上一个局面的结果。
- [x] JavaFX 主界面显示最多五栏且不被引擎输出线程阻塞。

**验证：** 6 项工作区测试覆盖代次隔离、MultiPV、共识/分歧、独立暂停/恢复、排序/隐藏、慢管道、慢 UI 通知合并和关闭取消；15 项相关测试通过。Windows UI Automation 使用官方 Pikafish 2026-01-02 同时启动 5 个真实 UCI 进程，五栏均返回 PV1/PV2，控件、共识、零网络端点和停止/关窗零残留均通过。
**依赖：** 9。
**文件：** 不超过 5 个 UI/视图模型/测试文件。

## 11. 分支棋谱领域模型（M）

**说明：** 建立不可变节点、主线、变例、注释和评估的数据模型。

**验收：**
- [x] 插入、删除、换主线、跳转和合并有单元测试。
- [x] 保存/加载往返不丢失节点身份或注释。
- [x] 非法着法不能进入树。

**验证：** `GameTreeTest` 8 项测试覆盖主线/变例、整条合并原子性、子树删除、跳转、不可变节点、UUID/注释/评估往返、损坏/超限文件和错方/畸形/非法棋步/送将拒绝；完整 `mvn verify` 93/93 通过。
**依赖：** 3。
**文件：** `game/tree` 下不超过 5 个生产/测试文件。

## 12. 分支树与评估界面（M）

**说明：** 提供树形浏览、注释、评估曲线和关键失误跳转。

**验收：**
- [x] 点击节点准确恢复局面和引擎上下文。
- [x] 主线与变例有清晰视觉区别。
- [x] 大棋谱仍可交互。

**验证：** `GameTreeViewModelTest` 7 项导航、评估、损坏路径和 10,000 手压力测试（约 0.4 秒）+ 主窗口 UI Automation 节点跳转/局面恢复/零网络验收 + 完整 `mvn verify` 100/100 通过。
**依赖：** 10、11。
**文件：** 5 个 UI/测试文件（视图模型、窗格、主控制器、FXML、测试）。

## 13. 连接配置向导（M）

**说明：** 把窗口选择、棋盘框选、方向、DPI、主题、模型、点击和测试合并成可复用配置。

**验收：**
- [x] 向导完成前不会开启自动点击。
- [x] 配置可导入导出，敏感路径可脱敏。
- [x] 目标变化时提示重新校准。

**验证：** `ConnectionWizardStateTest` 6 项覆盖五步闭锁、版本迁移、严格格式、脱敏/本机导出、目标变化和本地测试棋盘端到端干运行；JavaFX UI Automation 验证无目标/误选目标均保持禁用，且真实窗口身份准确显示。
**依赖：** 8。
**文件：** 不超过 5 个 profile/UI/测试文件。

## 14. 时间策略内核（M）

**说明：** 按比分、阶段、剩余时间、步数和复杂度产生思考预算。

**验收：**
- [x] 决策是纯函数、可解释、确定性。
- [x] 永不超过总剩余时间和安全余量。
- [x] 所有阈值边界有测试。

**验证：** 参数化单元测试。
**依赖：** 3。
**文件：** `profile/time` 下不超过 5 个生产/测试文件。

## 15. 安全智能脚本 DSL（M）

**说明：** 提供事件—条件—动作规则，只调用应用内白名单动作。

**验收：**
- [x] 无 shell、反射、文件任意路径或网络任意 URL 能力。
- [x] 规则有语法验证、预演、限步和执行日志。
- [x] 脚本不能绕过自动化授权状态机。

**验证：** 注入/资源耗尽/权限绕过测试。
**依赖：** 4、14。
**文件：** `profile/script` 下不超过 5 个生产/测试文件。

## 16. 棋谱格式适配器（M）

**说明：** 固定 PGN/XQF/CBR/TXQ 的输入输出契约和错误处理。

**分片进度：** PGN、XQF 1.0、CBR v2 与 TXQ/PDTX 1 内核及统一界面文件网关均已完成；四格式错误、警告、结果、隐藏元数据和原子保存已接线。CBR 候选导出仍须官方 CCBridge 黑盒验证。

**验收：**
- [x] 每种格式有合法、损坏、超大样例。
- [x] 往返保持各格式可表示的主线、变例、结果和注释；XQF 1.0 不支持的变例会拒绝而非拍平。
- [x] 不支持字段有显式报告。

**验证：** `PgnAdapterTest` 19 项、`Xqf10AdapterTest` 28 项、`Cbr2AdapterTest` 25 项、`TxqAdapterTest` 23 项、`ManualFileGatewayTest` 10 项；完整 `mvn verify` 296/296 通过。真实 JavaFX 主窗口可访问棋谱菜单并正常关闭，引擎收到 `quit`、无进程残留；系统保存对话框因多屏 DPI 自动化树不稳定，未计作逐项点击验收。
**依赖：** 11。
**文件：** 每次只处理一种格式、最多 5 个文件。

## 17. 开局库批处理（M）

**说明：** 批量导入、去重、查询、生成和校验 XQB/PF/BH/OBK。

**验收：**
- [ ] 支持进度、取消、断点和错误明细。
- [ ] 同一输入得到确定性输出。
- [ ] 超大/损坏库不会耗尽内存。

**验证：** 固定样例、重复项和取消恢复测试。
**依赖：** 16。
**文件：** 每种格式一个切片，每次不超过 5 个文件。

## 18. 引擎比赛（M）

**说明：** 循环赛/淘汰赛、时间控制、断点恢复和结果导出。

**验收：**
- [ ] 赛程确定、颜色平衡、异常引擎判负规则明确。
- [ ] 重启后不重复或丢失已完成对局。
- [ ] 导出 PGN/CSV 和汇总统计。

**验证：** 假引擎确定性联赛集成测试。
**依赖：** 9、14、16。
**文件：** 每次一个赛制切片，不超过 5 个文件。

## 19. 语音与主题（M）

**说明：** 中文语音事件和用户导入 SVG/PNG 棋子主题。

**验收：**
- [ ] 语音可关闭、不会阻塞走棋线程。
- [ ] 主题包有尺寸、格式和路径校验。
- [ ] 无效主题自动回退到内置主题。

**验证：** 主题验证测试 + 语音队列测试 + DPI 手工检查。
**依赖：** 1。
**文件：** 每次只做语音或主题，不超过 5 个文件。

## 20. 可靠性矩阵（M）

**说明：** 对 DPI、主题、方向、动画、失焦、移动和长局运行统一 E2E。

**验收：**
- [ ] 1,000 半回合零错误落子。
- [ ] 所有异常场景安全暂停且原因正确。
- [ ] 紧急停止延迟 ≤100ms。

**验证：** 生成带环境信息和失败证据的 E2E 报告。
**依赖：** 8、13、14。
**文件：** E2E 配置、运行器、报告器和文档，不超过 5 个文件。

## 21. 安全、依赖与许可证审计（M）

**说明：** 审查依赖、二进制、模型、文件解析、外部进程和隐私日志。

**验收：**
- [ ] 无未处理的可达高危/严重漏洞。
- [ ] 所有二进制/模型有来源、许可、版本和哈希。
- [ ] 自动化边界无法被配置或脚本绕过。

**验证：** Maven 依赖报告、秘密扫描、许可证与威胁模型清单。
**依赖：** 所有 P0/P1 功能任务。
**文件：** 审计配置与报告，不超过 5 个文件。

## 22. Windows 打包与首次启动（M）

**说明：** 生成自带运行时的 Windows 包和安全的首次启动向导。

**验收：**
- [ ] 干净 Windows 环境无需全局 Java 可启动。
- [ ] 首次启动不默认下载/运行未知引擎或启用自动化。
- [ ] 卸载不删除用户棋谱，升级保留并迁移配置。

**验证：** Windows Sandbox 安装、升级、卸载测试。
**依赖：** 21。
**文件：** 打包配置、启动器、迁移测试和用户文档，不超过 5 个文件。

## 23. VIP 对等验收与发布候选（M）

**说明：** 逐行关闭功能矩阵，形成 v1.0.0 发布候选。

**验收：**
- [ ] 所有 P0/P1 行为均有证据并标记通过。
- [ ] 全量测试、打包和干净环境验证成功。
- [ ] 已知限制、许可证、隐私和安全范围写入发布说明。

**验证：** `.\mvnw.cmd --batch-mode verify` + Windows Sandbox 验收清单。
**依赖：** 20、21、22。
**文件：** 对照矩阵、发布说明、变更日志和验收报告。

## 24. 只读实时陪练（M）

**说明：** 选择用户自有或明确授权的本地人机窗口，只读识别红黑棋子、朝向和已发生着法；同步本地棋谱并显示 Pikafish 候选，但不向目标窗口发送输入。

**验收：**
- [x] 默认模式为“只读陪练”，只读连接不创建、不武装输入协调器。
- [x] 自动翻转棋盘，并在观察到首步后按实际移动棋子纠正红黑行棋方。
- [x] 普通着法和吃子各同步一次；歧义、遮挡、低置信度或窗口变化时暂停。
- [x] PV1 只作高亮预演，不提前写入真实棋谱。

**验证：** 定向 JUnit 37/37 + 完整 `verify` 共 377 个测试、0 失败、4 个显式验收测试跳过 + 本地测试棋盘红/黑方向模型实机验收 + Windows app-image 重建与 234 项 SHA-256 复核。目标窗口选择必须由验收者亲自点击，合成鼠标事件按设计不被选择器接受。
**依赖：** 5、6、8、13。
**文件：** 每个实现切片不超过 5 个生产/测试文件。

## 25. 流畅只读工作流（M）

**说明：** 参考鲨鱼公开设置体现的快速连接与持续状态反馈，但保持 PikaDesk 每次重新识别、只读零输入和失败闭锁的独立实现。

**验收：**
- [x] 只读模式在模型定位成功时自动完成预校准，省去一次重复的干运行操作。
- [x] 自动走棋仍必须手工干运行，不共享只读快速路径。
- [x] 主窗口独立显示连接生命周期，不被引擎 PV 文本覆盖。
- [x] 建立识别耗时基线；只优化经测量的瓶颈，不放宽识别门槛。

**验证：** 定向 JUnit 与完整 `verify` 共 386 项测试、0 失败、4 项按条件跳过；本地测试棋盘 20/20 走子确认，平均 380 ms、P95 416 ms、最大 447 ms；Windows app-image 重建，234 项 SHA-256 复核、默认引擎 UCI/就绪握手、窗口启动与零残留进程均通过。
**依赖：** 24。

## 26. 第二阶段：通用窗口识别 + 授权自动执行（M）

**说明：** 列出当前可见顶层窗口，由用户选择自有或已获得明确授权的棋盘；复用现有安全内核自动执行引擎着法并视觉确认。

**验收：**
- [x] 通用窗口列表有界、文本净化，且排除自身、不可见、最小化和异常尺寸窗口。
- [x] 用户可从列表选择或回退到准星点选；伪造/过期令牌被拒绝。
- [x] 两种选择方式都必须经过干运行、本次授权、目标复核和视觉确认。
- [x] 没有公共平台名称、窗口特征、坐标模板或专用适配器。

**验证：** 30 项定向 JUnit；真实窗口目录找到并解析 `1120x840` 本地棋盘；失焦零点击拒绝；红方 20/20（平均 376 ms、P95 413 ms）与黑方单步视觉确认通过；全量 393 项测试 0 失败、4 跳过；app-image 234 项哈希复算一致。
**依赖：** 7、8、13、25。
